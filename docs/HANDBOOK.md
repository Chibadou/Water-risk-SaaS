# HANDBOOK — notes de session pour HydroVigie

> Fichier de passation : concepts clés, pièges connus, état du projet et prochaines étapes.
> **À maintenir à la fin de chaque session de travail.** Dernière mise à jour : 2026-08-07 (import d'outillage ECC, puis neuf échecs silencieux trouvés et corrigés — hors sprint, `main` non touché).

## 1. Le projet en une minute

SaaS de suivi du **risque eau quantité** par site (adresse précise), France. Next.js 16 (App Router, TS, Tailwind 4) sur Vercel, prod : **`https://water-risk-saa-s.vercel.app`** (alias de production, confirmé actif le 2026-07-20 après merge de `main`). ⚠️ Les URLs de déploiement à hash (`…-chibadous-projects.vercel.app`) sont protégées par Vercel Authentication (redirect SSO 302 pour tout visiteur non connecté au compte) — ne pas les utiliser comme lien public ni pour les probes runner ; toujours viser l'alias de prod. Plan produit complet : [`PLAN.md`](./PLAN.md) · roadmap : [`SPRINTS.md`](./SPRINTS.md) (**sprints 1→32 livrés et mergés dans `main`**, dernière mise en prod le 2026-08-06 : la carte des ressources, sprints 29→32).

**Fin de session 2026-07-30** — Sprint 20 (indice d'anticipation) mergé vers `main`, suivi de deux follow-ups sur la même branche : (1) lien sortant vers la prévision officielle **MétéEAU des nappes** (BRGM) — l'API est OAuth2-gated, décision utilisateur de renvoyer par un lien plutôt que ré-héberger ; (2) **export PDF** du rapport ESG (site + portefeuille) via impression navigateur, sans dépendance nouvelle. L'indice d'anticipation a aussi été **validé sur données réelles** (escape hatch Actions, mode diag `anticipation`) : résultat non dégradé et cohérent sur un site réellement sous restriction (Perpignan, Têt aval). Trois merges vers `main` cette session (tous vérifiés build+lint+tests+e2e avant push). Branche de session : `claude/water-restrictions-prediction-46uaeh`. Prochaine session : voir §5.

**Fin de session 2026-07-30** — **Sprint 21 : jours d'activité contrainte.** Branche `claude/water-restriction-risk-tool-td2yss`. L'outil répond enfin à la question qui déclenche une décision d'exploitation : combien de jours par an l'activité est freinée, aujourd'hui, d'ici la fin de l'étiage, et en 2050. **Correction majeure en cours de sprint, sur revue utilisateur** : une première version pondérait avec une table « secteur × niveau » calibrée à la main — écartée au profit d'une **lecture des mesures réellement prescrites** dans les arrêtés (« ne pas inventer de coefficients »). Ajout de la dimension **origine de l'eau** (corrige le biais `worstLevel` = max sur SUP/SOU/AEP) et refonte du bloc BNPE en **panneau d'arbitrage** usage × milieu. Build/lint clean, 12 suites au vert (2 neuves), 12/12 e2e. **Restent à valider en conditions réelles sur Vercel** (egress bloqué en dev) : les horizons *année type* et *fin de saison* dépendent de `/api/history`, injoignable en bac à sable — seules les restrictions (données embarquées) sont vérifiables localement, et elles le sont. Prochaine session : voir §5.

**Sprint 22 (même session)** — Robustesse du modèle de jours. Fenêtre d'historique **5 → 10 ans** (coût mesuré au banc : 964 ms → 1601 ms, budget 60 s ; surchargeable par `HISTORY_WINDOW_YEARS`), découpage mensuel **par niveau** (`parMoisNiveau`), et deux pistes du backlog **closes par constat négatif** : ZRE hors métropole (aucune couche plus large n'existe) et Explore2 QMNA5/recharge (absents de cette collection). Badge Sprint 22.

**Sprint 23 (même session)** — **SWI Météo-France** intégré comme précurseur le plus précoce de l'indice d'anticipation (climatologie embarquée, mois courant live), et **granularité ESRS E3** portée à un tableau point-par-point distinguant ce que l'outil fournit de ce que l'entreprise doit fournir. Badge Sprint 23.

**Sprint 24 (même session)** — **Bassin & agence de l'eau** (35 186 communes, référentiel Sandre) sur la fiche site, **horizons portefeuille** étendus (saison en climatologie seule + 2050, sans appel amont supplémentaire), et clôture motivée des derniers items du backlog. Badge Sprint 24.

**Sprint 25 (même session)** — **Rattachement aquifère BDLISA** : dernier item ouvert du backlog, débloqué en changeant la question (appartenance ensembliste au lieu du choix d'une entité). Badge Sprint 25. **Le backlog « chantiers de données » est désormais soit livré, soit bloqué avec motif écrit.**

**Mise en prod 2026-07-31** — Sprints 21→25 mergés vers `main` sur demande explicite. ⚠️ **Le diagnostic prod (`data/diag-request.json` mode `prod`, run 19) a attrapé un bug que le bac à sable ne pouvait pas voir** : `/api/swi` répondait 200 mais « aucune mesure récente » — les fichiers décennaux Météo-France sont des **`.csv.gz`** où le gzip fait partie de la charge utile (pas un `Content-Encoding`), donc `fetch` ne le déballe pas et `res.text()` rendait du binaire. Le script de build le gérait, la route non. Corrigé (`gunzipSync` sur détection des octets magiques) et remergé. **Leçon générale : toute route qui consomme une ressource data.gouv doit renifler le gzip, pas se fier au `Content-Type`.**

**Vérifié en réel sur la prod (run 19)** — enfin la validation qui manquait : `/api/history` répond `available: true` avec **`windowYears: 10`, 9 162 arrêtés parsés, couverture 2017-01-01 → 2026-07-31** (la fenêtre élargie du Sprint 22 tient sur le CSV réel de 11 Mo) ; `/api/restrictions` reproduit exactement la mesure locale (Eure-et-Loir SUP entreprise : 0 / 0,554 / 0,675 / **0,697**, 20 usages en crise, 3 non lus) ; `/api/bdlisa` renvoie 6 entités réelles emboîtées ; `/api/transition` donne Chartres → bassin H + ZRE, et **Ajaccio → bassin E résolu alors que la ZRE reste indisponible** (l'indépendance voulue entre les deux référentiels). Routes de compte toujours en 404, PMTiles en 206.

**Post-prod 2026-07-31 — ce que la vérif réelle a corrigé.** Trois défauts que le bac à sable ne pouvait pas voir, tous attrapés par le mode `prod` du diagnostic : (1) **`/api/swi` en échec silencieux total** — les fichiers Météo-France sont des `.csv.gz` (gzip dans la charge utile, pas un `Content-Encoding`) *et* `SWI_UNIF_MENS` est la **dernière colonne** avec des fins de ligne CRLF, donc `Number("0.949\r")` = NaN et **toutes** les lignes étaient écartées ; l'endpoint répondait 200 en disant « aucune mesure récente ». Le parseur vivait dans la route, donc rien ne pouvait le tester → **déplacé dans `lib/swi.ts` avec couverture de non-régression** (CRLF/LF, `;`/`,`, ordre des lignes, maille inconnue). (2) **Retard de publication du SWI** (voir §2) → garde-fou de fraîcheur. (3) **`urlenc()` défini dans la branche `hubeau`** du script de diag, donc absent en mode `prod` : l'URL de la sonde historique partait vide et renvoyait 400 — la sonde n'avait jamais rien vérifié.

**Vérifié en réel après correctifs** : `/api/swi` → maille à 3,1 km, indice 0,389 pour 2025-12, percentile 23, « Sols très secs », référence 1990-2019 ; `/api/history` sur de **vraies zones** (Lyon `84_69_0004`) → moyenne **46 j/an sur 9 années complètes**, `parMoisNiveau` présent, détail 2025 `{vigilance: 87, alerte: 28}`. Les mécaniques des Sprints 21-22 sont donc validées sur données réelles.

**Session 2026-08-04 — Sprint 26 : le portefeuille comme objet d'analyse.** Branche `claude/outil-portefeuille-sites-pertinence-1y4e3a`. Partie d'une question d'idéation (« comment rendre l'outil pertinent pour une entreprise multi-sites ? ») → **document de réflexion versé au repo** ([`IDEATION-PORTEFEUILLE.md`](./IDEATION-PORTEFEUILLE.md), 8 axes, chaque piste avec un verdict, + benchmark sourcé des modèles existants) puis implémentation des deux axes retenus par l'utilisateur. **Le tableau de bord empilait des analyses de site ; il analyse désormais le parc** : simultanéité rejouée sur les arrêtés réels, concentration, m³/€ à risque, et un **executive summary** en tête de page. **Non validé sur données réelles** (egress) — voir §5.

**Validé en réel le 2026-08-04 (diag Actions mode `app`, run 24)** — le rejeu portefeuille a tourné sur les vrais arrêtés via `scripts/diag/replay-portefeuille.ts`, et **a attrapé un bug de dénominateur que le bac à sable ne pouvait pas voir** (voir §2, « couverture du fichier »). Après correctif, sur un parc réel de trois sites très éloignés (Perpignan, Chartres, Lyon — **trois zones distinctes, 3 zones effectives indépendantes**) : invariant périodes↔agrégats **exact** sur les 3 zones, contrat de l'opt-in `?periodes=1` respecté (agrégats identiques, calendrier absent sans le paramètre), fenêtre 2017-2025 (9 années complètes), **26,2 j/an avec au moins deux sites contraints en même temps**, et surtout un **pic de 3 sites sur 3 simultanément contraints pendant 84 jours consécutifs à partir du 2023-08-04**. Trois sites à 600 km les uns des autres, dans trois bassins différents, arrêtés ensemble pendant près de trois mois : c'est précisément la démonstration que la fonctionnalité existe pour rendre visible, et qu'aucune somme de jours ne peut donner. Chartres partage **97 %** de ses jours contraints avec le reste du parc (site aggravant), Perpignan 26 % (diversifiant, parce qu'il est contraint bien plus souvent que les autres). ⚠️ `data/diag/` purgé après analyse, selon la convention.

**Mise en prod 2026-08-04 (Sprint 26)** — merge vers `main` sur demande explicite, puis **vérifié sur le déploiement réel** (diag mode `prod`, run 25). Deux sondes ciblées sur de **vraies zones** (`84_69_0004` Lyon, `24_028_0003` Chartres) et non sur `test` — qui ne correspond à rien et laisserait une fonctionnalité absente ressembler à un calendrier vide. Résultat : `?periodes=1` sert bien le calendrier (**12 et 10 runs**, identiques à la mesure du mode `app`), **sans le drapeau les périodes sont absentes**, et les **agrégats sont identiques entre les deux appels** — le contrat de l'opt-in tient en production. `coverage` 2017-01-01 → 2026-08-04, `windowYears` 10 : le correctif de dénominateur reçoit la bonne entrée en prod. Le delta de charge utile entre les deux appels est de **271 octets pour 22 runs** — le RLE est aussi compact que prévu. `data/diag/` purgé.

**Session 2026-08-05 — Sprint 27 : ressource en eau par site.** Deux questions posées : d'autres sources de restrictions, et un modèle de ressource disponible. La première a une réponse courte — **Propluvia est décommissionné, VigiEau est le canal officiel unique, il n'y a pas d'alternative live**. La seconde a levé un blocage vieux du Sprint 10. **Trois passes de sonde avant la moindre ligne de code**, dont deux ont changé la conception du modèle. Voir §2 pour le modèle et ses limites mesurées.

**Session 2026-08-05 — Sprint 28 : deux dénominateurs, deux questions.** Né d'une question de revue de l'utilisateur — « ces 2 points ne nécessitent pas une correction du modèle ? » — et **la réponse était oui**. Le correctif du Sprint 27 traitait le symptôme : il retirait la classe WRI au-delà de 100 %, là où le résultat crevait les yeux, alors que le défaut existait à tous les niveaux. Le modèle sépare désormais la **pression sur le cours d'eau** de l'**autonomie du territoire**. Gain de couverture non cherché au passage. ⚠️ **Leçon de méthode** : trouver le bon fait ne suffit pas, encore faut-il en tirer le bon correctif — j'avais patché là où c'était visible sans me demander ce que le chiffre valait ailleurs.

**Mise en prod 2026-08-06 (sprints 29→32, la carte des ressources)** — merge de `claude/france-map-water-data-2oe6h6` vers `main` sur demande explicite, après build + lint + **18 suites** + **60/60 e2e**. Ce qui part en prod : la page `/carte` (stations de débit, piézomètres, ONDE, ouvrages BNPE, captages AEP, nappes affleurantes, cours d'eau, plans d'eau), les popups au clic et **l'état de l'objet cliqué**. ⚠️ Au moment du merge, **trois choses n'avaient jamais été vues sur le déploiement réel** et sont à vérifier après la mise en prod, pas à supposer : (1) les **13,8 Mo de `data/refdata/*.geojson`** neufs (nappes, cours d'eau, plans d'eau) ne sont servis que si `outputFileTracingIncludes` les embarque — les trois entrées **sont bien déclarées** dans `next.config.ts`, mais le tracing n'a jamais été **constaté** sur un déploiement, et un oubli ici ne casse que la prod ; (2) le **fond de carte raster** est injoignable en bac à sable, donc la carte n'a jamais été vue avec ses tuiles ; (3) `/api/carte/etat` n'a été chronométré que **depuis un runner GitHub**, jamais depuis Vercel, alors que la prod s'avère **3 à 5× plus lente que le runner** vers Hub'Eau (`/api/hydro` 16,0 s, `/api/piezo` 11,0 s au run 39 — cf. §5) : c'est la même source, et le budget de 6 s de la référence est donc à réarbitrer sur une mesure prise en prod.

**Vérifié en prod le 2026-08-06 (diag `prod`, run 40) — les trois inconnues sont levées, et la principale dans le bon sens.** (1) **`outputFileTracingIncludes` embarque bien les trois GeoJSON** : `/api/nappes` sert les **621 masses d'eau, 2,35 Mo** en 0,27 s, `/api/cours-eau` et `/api/plans-eau` répondent sur bbox avec des entités réelles autour de Chartres (5 tronçons de l'Eure, 10 plans d'eau dont « Gravière 5 de la Séverine », 6,03 ha). (2) `/api/carte` répond en **7,8 s** avec **10 stations hydro, 24 piézomètres, 13 points ONDE, 300 ouvrages et 115 captages AEP** — les mêmes ordres qu'au runner. (3) ⚠️ **La crainte tirée du run 39 était infondée : le budget de 6 s tient en prod.** `/api/carte/etat` répond **avec sa référence** — débit en **2,7 s** (« Débit au-dessus de l'étiage », VCN10 sur 10 ans), piézomètre en **3,1 s** (« Nappe proche des normales (IPS) », 26 ans, série de 35 points), ouvrage en 0,17 s (730 271 m³ en **2023**, eau potable), zone en 0,50 s (Eure Moyen haut, alerte renforcée sur AEP/SOU/SUP). **La lenteur mesurée sur `/api/hydro` ne se transporte donc PAS à `/api/carte/etat`** — c'était bien le travail supplémentaire de la route (référentiel de bbox + 8 candidates), pas le réseau sortant. ⚠️ Reste vrai : **le chemin « référence abandonnée » n'a toujours jamais été exécuté en réel**, ni au runner ni en prod. ⚠️ Reste non vu : **le fond de tuiles raster**, qu'aucune sonde HTTP ne peut juger — seul un œil sur `https://water-risk-saa-s.vercel.app/carte` le dira.

**Session 2026-08-05 — Sprint 32 : l'état des sources sur la carte.** Les popups disaient ce qu'est un objet, jamais où il en est. Elles portent désormais les **mêmes chiffres que la fiche site** (mesure, tendance, référence IPS/VCN10, sparkline), le **dernier volume déclaré avec son année** pour les ouvrages, et le **niveau réglementaire** pour les masses d'eau. ⚠️ Deux règles rendues structurelles : un **service injoignable** ne se dit pas comme une **station muette**, et la référence est **abandonnée après 6 s** plutôt que de bloquer la popup 15 s. ⚠️ L'enrichissement a fait revenir le défaut du Sprint 31 par une autre porte — popup trop haute passant sous le bouton flottant — corrigé et **remesuré en 390×844**.

**Session 2026-08-05 — Sprint 31 : la carte répond « d'où vient mon eau ? ».** Quatre points signalés depuis un téléphone. La légende flottante est **supprimée** (elle dupliquait la barre de bascules en masquant la carte), les couches sont **groupées par question** et **décrites dans la page**, et deux couches s'ajoutent : **plans d'eau** et **captages d'eau potable**. ⚠️ Les captages n'ont demandé **aucune source nouvelle** — l'usage était déjà publié sur les chroniques BNPE, à joindre par `code_ouvrage` (couverture mesurée 82-100 %). ⚠️ Le sondage a corrigé le code avant livraison : les chroniques font 16-18 k lignes contre 1,8-3,1 k ouvrages, donc la taille de page du référentiel aurait perdu la plupart des usages. Rendu mobile **revérifié en 390×844 avec une popup ouverte**.

**Session 2026-08-05 — Sprint 30 : lisibilité de la carte.** Trois demandes utilisateur après avoir vu la carte : dégrouper les ouvrages superposés, pouvoir lire le nom et les caractéristiques d'un objet au clic, ajouter les cours d'eau. Livré. **Le sondage a de nouveau changé la conception** : deux risques de champs laissés ouverts au Sprint 29 sont refermés (`libelle_site` et les libellés d'observations ONDE **existent**), `urn_bss` s'avère contenir une **URL** alors que l'hydrométrie ne publie **aucune** URI de station, `Karstique` n'est **pas renseigné** sur les 200 masses d'eau sondées, et `LongueurTotKm` porte des **unités incohérentes**. Pour les cours d'eau, la simplification s'est révélée quasi inopérante (‑26 % pour 8× de tolérance) : le coût est le nombre d'entités, d'où **fichier complet sur disque + filtrage par bbox à la requête** — 5,84 Mo stockés, ~50 Ko envoyés.

**Session 2026-08-05 — Sprint 29 : la carte des ressources.** Branche `claude/france-map-water-data-2oe6h6`. Nouvelle page `/carte` : les objets **physiques** de la ressource autour d'une adresse (stations de débit, piézomètres, ONDE, ouvrages BNPE) sur fond de nappes affleurantes. **Trois sondages avant la moindre ligne de code produit**, et deux ont changé la conception : les ouvrages BNPE ont bien des coordonnées (**item 8 bis levé**) mais le référentiel avoue lui-même que certaines sont le **centroïde de la commune** ; et le WFS Sandre filtre *quelles* entités il renvoie, **jamais leur résolution** (237 Mo en national, 19,5 Mo pour un seul viewport), ce qui a fait tomber **les deux** options du plan. ⚠️ **Le sprint a aussi trouvé un bug d'affichage vieux de six sprints** — `map.on("load")` n'installe aucune couche quand le fond de carte est injoignable — et il n'a été trouvé qu'en **regardant la page rendue**, jamais par une sonde de nombres. Voir §5 pour la méthode.

**Mise en prod 2026-08-06 (session UI/UX, sprints 33→37 + protocole lecteur d'écran)** — merge
`a032ddf` de `claude/project-ui-ux-audit-b7e8a3` vers `main`, **sur demande explicite**, après build
+ lint clean, **20 suites** et **62/62 e2e rejoués sur `main`**. ⚠️ **Ce qui part en prod n'a jamais
été vu avec de vraies données** : l'egress est bloqué en bac à sable, la refonte entière a été
constatée à travers des bouchons Playwright. Trois choses sont donc à **vérifier sur le déploiement,
pas à supposer** : (1) les cinq chapitres de la fiche site **peuplés** — 9 des 12 blocs migrés n'ont
jamais été rendus avec leurs données ; (2) le comportement du **sommaire collant** pendant qu'un bloc
s'insère au-dessus de la position de lecture, jamais observé ; (3) le chargement sous les **16,0 s
réelles** de `/api/hydro`, alors que les délais simulés plafonnaient à 5 s. S'y ajoute le **test
humain au lecteur d'écran**, que [`CHECK-LECTEUR-ECRAN.md`](./CHECK-LECTEUR-ECRAN.md) rend faisable
mais ne remplace pas.

**Session 2026-08-06 — Audit UI/UX à œil neuf, puis sprints 33→37.** Branche
`claude/project-ui-ux-audit-b7e8a3`. Partie d'une demande d'audit « carte blanche » → document versé
au repo ([`AUDIT-UI-UX.md`](./AUDIT-UI-UX.md), 10 constats P1→P10 avec leurs preuves chiffrées et
leurs fichiers), puis **quatre questions à l'utilisateur** avant toute ligne de code, puis les cinq
sprints qu'il a arbitrés. **Le produit avait été construit panneau par panneau ; personne n'avait
jamais repris la page dans son ensemble** — et la rigueur des données n'était pas rendue par
l'interface. ⚠️ **Limite majeure de toute la session, valable pour les cinq sprints : l'egress est
bloqué en bac à sable, donc TOUT a été vu à travers des bouchons Playwright.** Les chiffres affichés
sur les captures sont inventés ; ce qui est vérifié, c'est la mise en page, les parcours et les
invariants — jamais une réponse VigiEau réelle. **La première chose à faire à la prochaine session
est de regarder la preview Vercel**, pas d'empiler un sixième sprint.

| Sprint | Ce qu'il corrige | Mesure |
|---|---|---|
| 33 | 31 cartes identiques ⇒ `ui/Panel` à 4 variantes ; contrastes | `text-slate-400` **69 → 0** ; 10-11 px **17 → 0** |
| 34 | La fiche répondait à son propre H1 **en 4ᵉ position** ⇒ synthèse rédigée + 5 chapitres ancrés | débordement 390 px **145 → 0** |
| 35 | 7 requêtes, aucun repère ⇒ squelettes dimensionnés + bandeau de sources | déplacement du chapitre 4 : **59 px** sur 9 512 |
| 36 | Le champ d'adresse était inutilisable au clavier ⇒ combobox ARIA complet | `/sites` **38-40 → 0 px** ; 62/62 e2e |
| 37 | 26 sections de méthodologie, **aucune ancre** ⇒ registre typé | **26/26** ancrées, 9 panneaux recâblés |

**Suite de la même session — protocole de vérification au lecteur d'écran.** Le sprint 36 avait
livré le balisage d'accessibilité **sans qu'aucun lecteur d'écran réel n'ait été utilisé**, limite
écrite dans son propre compte rendu. [`CHECK-LECTEUR-ECRAN.md`](./CHECK-LECTEUR-ECRAN.md) ferme
l'écart : **10 écrans téléphone** (390 × 844) — cinq états de **données** choisis parce qu'ils se
ressemblent à l'œil et ne doivent surtout pas se ressembler à l'oreille, cinq états d'**interaction**
— chacun avec l'arbre ARIA réellement produit ([`captures/arbres-aria.md`](./captures/arbres-aria.md))
et ce qu'il faut **entendre**.

⚠️ **Construire ces dix écrans a trouvé quatre défauts que le sprint 36 avait manqués**, dont un qui
rendait **muet son correctif principal** :

| Défaut | Ce que l'arbre disait | Pourquoi invisible |
|---|---|---|
| `aria-label` sur un `<span>` **sans rôle** n'est **pas exposé** | `text: SUP SOU AEP` — **sans le niveau** | Le correctif P5 (« plus d'encodage par la couleur seule ») ne corrigeait rien. Il faut `role="img"`. |
| Code de zone concaténé au nom dans le titre | `heading "Eure Moyen haut24_028_0003"` | Séparé par une marge à l'écran, collé dans le nom accessible. |
| Composantes non estimées lues « tiret », ou rien | `text: … (12,5 %) —` | « Une absence n'est jamais un zéro » n'était tenu **qu'à l'œil**. |
| Émoji de secteur prononcé | `heading "🏭 Impact pour…"` | Décoratif à l'écran, contenu dans l'arbre. |

⚠️ **Le test humain reste à faire** : le protocole le rend possible, il ne le remplace pas. Deux
points ne sont pas testables sans appareil — le comportement de `aria-activedescendant` sur VoiceOver
(risque n° 1) et le fait que **8 s** suffisent à atteindre « Annuler la suppression ».

**Trois leçons durables de cette session** (détaillées dans les comptes rendus datés) :

1. **Une source EN ATTENTE n'est ni un fait ni un manque.** La synthèse affirmait, trois secondes
   après l'ouverture, que « la projection 2050 n'est pas disponible pour ce bassin » — puis se
   dédisait. `undefined` signifiait deux choses (« la réponse a dit non » / « la réponse n'est pas
   arrivée ») ; il faut les séparer explicitement. **C'est la même règle que le sprint 32 avait rendue
   structurelle sur la carte** (« service injoignable ≠ station muette »), réapparue par deux autres
   portes.
2. **L'e2e attrape ce qu'aucune capture ne montre.** Un `aria-label` perdu pendant une migration
   (une `<section>` sans nom **cesse d'être un landmark**, à pixel identique), trois changements de
   contrat au sprint 36, et un lien qui « fonctionne » mais mène au mauvais endroit au sprint 37.
3. **Regarder la page rendue attrape ce qu'aucun test ne montre.** « dont **1 jours** », « nappe :
   nappe proche des normales (**ips**) », et 145 px de défilement latéral — tous trouvés à l'œil ou
   au ruban, aucun par un test unitaire.
4. **⚠️ Un attribut d'accessibilité présent dans le DOM n'est PAS un attribut exposé.** `aria-label`
   sur un élément de rôle `generic` (un `<span>` nu) est **ignoré** dans le calcul du nom accessible.
   Vérifier la présence de l'attribut dans le source ne prouve rien. **L'arbre ARIA
   (`locator.ariaSnapshot()` en Playwright, ou le panneau Accessibility de DevTools) est le seul
   intermédiaire fiable entre le code et le lecteur d'écran** — à regarder à chaque sprint qui touche
   au balisage.

⚠️ **Piège d'environnement, rencontré une dizaine de fois** : après `npm run build`, un `next start`
déjà lancé continue de servir **l'ancien manifeste de chunks** et la page casse en `ChunkLoadError` —
l'e2e échoue alors sur des sélecteurs **sans rapport**, ce qui envoie chercher un bug d'interface
inexistant. Recette qui marche : `pgrep -f next-server | xargs -r kill -9`, attendre **5 s** que le
port se libère (sinon le nouveau serveur échoue en `EADDRINUSE` en silence et l'ancien continue de
répondre), parfois `rm -rf .next`, puis lancer avec **`setsid nohup … & disown`** — sans cela le
serveur est tué à la fin de l'appel shell. ⚠️ **`pkill -f "next"` est refusé** par le harnais (le
motif attrape le processus CLI).

**Session 2026-08-07 — revue du dépôt [ECC](https://github.com/affaan-m/ECC) et import sélectif
d'outillage agent (hors sprint).** Branche `claude/ecc-github-content-review-ua4nci`, **aucun code
produit modifié**. Verdict : **ECC ne contient aucun contenu métier eau** — c'est de l'outillage pour
agents de code (67 agents, 284 skills, 122 fichiers de règles). **Six fichiers retenus sur 351**
(1,7 %), tous parce qu'ils répondent à un défaut **déjà payé ici** : `silent-failure-hunter` (cf. le
`/api/swi` qui répondait 200 en écartant toutes ses lignes), `type-design-analyzer` (le `undefined`
du Sprint 35 qui signifiait deux choses), `a11y-architect` + les skills `accessibility` /
`frontend-a11y` (prolongent `CHECK-LECTEUR-ECRAN.md` en apportant les critères WCAG 2.2 qui
manquaient — Target Size 24×24, Focus Appearance), et `click-path-audit` (handlers qui s'annulent
entre eux — le mode d'échec exact du bug de six sprints trouvé au Sprint 29). ⚠️ **Deux skills
écartés pour redondance affaiblissante** — `verification-loop` et `rules/common/testing.md`
s'arrêtent à « build + lint + tests + 80 % de couverture », c'est-à-dire **moins** que la §6
ci-dessous, et ce dépôt a précisément un problème que la couverture ne mesure pas : du code qui
**passait** ses tests et était **faux en prod**. ⚠️ **`nextjs-turbopack` écarté pour contradiction**
avec `AGENTS.md` (« This is NOT the Next.js you know »). ⚠️ **Aucun des six fichiers n'a encore
tourné sur ce dépôt** : leur utilité est argumentée, pas démontrée — la mise à l'épreuve est
l'étape 1 du compte rendu. Tri motivé, licence MIT et réserves d'usage :
[`.claude/README.md`](../.claude/README.md) · compte rendu :
[`2026-08-07-import-outillage-ecc.md`](./comptes-rendus/2026-08-07-import-outillage-ecc.md).

**Session 2026-08-07 (suite) — neuf échecs silencieux, trouvés puis corrigés.** Même branche. Deux
passes de `silent-failure-hunter` (la seconde sur la couche de calcul, les composants React et les
scripts de build), **chaque constat recoupé à la main** avant correction. ⚠️ **La règle du Sprint 32
n'avait jamais quitté la carte** : elle est désormais tenue dans `lib/hubeau.ts` (drapeaux
`unreachable` par station et `serviceDegraded` sur la charge utile), `lib/bnpe.ts`, `lib/onde.ts`,
`lib/swi.ts`, `TransitionRiskPanel`, et **jusqu'au score composite et au rapport ESG**
(`ScoreInputs.indisponibles`). ⚠️ **Deux défauts n'avaient rien à voir avec le réseau, et sont
peut-être les pires** : un `?? 0` **à l'intérieur d'un `map`** faisait qu'une zone absente de
l'archive contribuait « 0 jour de restriction » au maximum — donc un « 0 j en alerte+ » affirmatif,
lu comme « jamais restreint » (`HomeClient` **et** `SitesDashboard`) ; et un `dispatchEvent` placé
**dans** le `try` après `setItem` faisait que « Ajouter à mes sites » ne faisait **rien du tout**,
sans message, quand le stockage local était plein — le commentaire affirmait « l'UI garde son état en
mémoire », or il n'y a pas d'état en mémoire. ⚠️ **Trois scripts de build sortaient en code 0 après
avoir tout jeté**, donc la CI committait un jeu vide ou périmé sous une coche verte : planchers de
vraisemblance ajoutés. ⚠️ **L'arithmétique du score n'a délibérément pas bougé** — une composante
injoignable reste hors de la moyenne pondérée ; seul le texte change, et un test l'exige.
**Vérifications** : build + lint clean, **22 suites** (2 neuves, dont un serveur bouchon qui exécute
vraiment le chemin dégradé de Hub'Eau), **62/62 e2e**. ⚠️ **Rien n'a été vu sur données réelles**
(egress) : les cinq nouveaux messages n'ont jamais été lus à l'écran. Compte rendu :
[`2026-08-07-echecs-silencieux.md`](./comptes-rendus/2026-08-07-echecs-silencieux.md).

**Décision structurante (utilisateur, Sprint 2, renforcée le 2026-07-20)** : *local-only*. Pas de compte **du tout** — pas de login, pas de serveur d'identité, aucune donnée utilisateur côté serveur. Les sites vivent en localStorage. Le code comptes/alertes/API (magic link Supabase, cron Resend, API v1) qui existait en opt-in a été **entièrement retiré** au Sprint 8 sur décision de l'utilisateur (« je ne veux pas de login »). Ne pas réintroduire de login sans demande explicite. Si des alertes email sont un jour souhaitées, le faire **sans login** (abonnement email type newsletter, cf. option écartée du Sprint 8).

**Compte rendu obligatoire en fin de sprint / de session de code** (convention posée le 2026-08-04, à la demande de l'utilisateur) : un fichier daté dans `docs/comptes-rendus/`, suivant **exactement** [`TEMPLATE-COMPTE-RENDU.md`](./TEMPLATE-COMPTE-RENDU.md) — sept sections, dans l'ordre, aucune omise. Il ne remplace ni ce HANDBOOK (concepts durables et pièges) ni `SPRINTS.md` (roadmap) : il raconte **une session**. ⚠️ Trois sections se dégradent en premier si on les laisse facultatives — **§3 erreurs potentielles** (un §3 vide à côté de code jamais confronté aux vraies sources est un compte rendu faux), **§5 état Git** (`main` touché ou non), et **§7 explication à un novice** (le lecteur sait programmer mais ne connaît ni ce dépôt ni la réglementation eau ; objectif : qu'il puisse rouvrir le code et le modifier lui-même). L'enforcement passe par `AGENTS.md`, seul fichier chargé automatiquement au démarrage. Exemple de référence : [`2026-08-04-sprint-26-portefeuille.md`](./comptes-rendus/2026-08-04-sprint-26-portefeuille.md).

**Workflow convenu** : développer sur la branche de la session courante (2026-07-21 : `claude/session-sdplfe`) → push → preview Vercel → retour utilisateur → sprint suivant. Chaque sprint est aussi poussé sur une branche de revue dédiée `sprint/NN-slug`. Mise en prod par **merge de la branche de session vers `main`** sur demande explicite (« push to prod ») — `main` porte le tree de la branche de session, donc le merge est propre même s'il n'est pas fast-forward (base = dernier sprint déjà mergé). Badge « Démo — Sprint N » dans `Shell.tsx` à incrémenter à chaque sprint. UI en français, code/commentaires en anglais.

## 2. Architecture — concepts clés

- **⚠️ Les trois états d'une source, et les trois `null` qui les confondaient** (2026-08-07) — toute
  source amont peut répondre **une mesure**, **« je n'ai rien pour cet endroit »**, ou **rien du tout
  parce qu'elle est en panne**. La confusion des deux derniers penche **toujours du même côté** :
  « pas de donnée » se lit « rien à signaler », donc « tout va bien ». Pour un outil dont le seul
  métier est d'alerter, c'est l'erreur maximale. Idiome retenu, à reprendre pour toute source neuve :
  un **type somme** (`Résultat | null | "service-error"`) plutôt qu'une exception — TypeScript refuse
  alors de compiler tant que le troisième cas n'est pas traité. ⚠️ **`undefined` n'est PAS libre côté
  `HomeClient`** : il y signifie déjà « en attente » (Sprint 35) et `null` « arrivé, rien à dire », ce
  qui interdit de réutiliser l'idiome `null`/`undefined` pour la panne — d'où les états parallèles
  `indicatorsInjoignables` / `ondeInjoignable`.
- **Une composante injoignable est exclue du score, jamais mise à zéro** (2026-08-07,
  `ScoreInputs.indisponibles`) — le chiffre et la couverture sont **identiques** qu'une source soit
  muette ou en panne ; seuls le libellé de la composante et le détail de confiance changent. ⚠️ **La
  confiance nomme la panne même en niveau « haute »** : sinon la seule réserve qui compte est
  exactement celle que le badge masque. ⚠️ Mettre un `0` à la place serait affirmer « aucun risque »
  sur une mesure jamais obtenue — l'affirmation la plus forte de l'outil, sur son plus grand trou.
- **⚠️ `?? 0` à l'intérieur d'un `map` avant un `Math.max` est un piège récurrent** (2026-08-07) —
  `Math.max(0, ...codes.map((c) => zones[c]?.jours ?? 0))` : le `0` extérieur est une borne légitime,
  celui de l'intérieur transforme une **zone non appariée** en zone jamais restreinte. La règle « une
  absence n'est jamais un zéro » se viole surtout **par commodité d'écriture**, pas par décision.

- **Le cadre de carte unique et ses 4 variantes** (Sprint 33, `components/ui/Panel.tsx`) — 31 blocs
  répétaient la même classe, si bien qu'un **arrêté préfectoral** avait exactement l'apparence d'un
  chiffre **modélisé** par l'outil et d'une **projection 2050** incertaine. `variant` rend visible la
  distinction que le code tenait déjà : `reglementaire` (liseré d'accent — le bloc citable),
  `modele`, `projection` (trait **discontinu** : le contour d'une chose incertaine ne doit pas
  paraître solide), `pedagogie`. ⚠️ **`tag` est opt-in** : automatique, il mettrait une pastille sur
  chaque sous-carte imbriquée. ⚠️ **`ariaLabel` n'est pas décoratif** — une `<section>` sans nom
  **cesse d'être un landmark**, à pixel identique (attrapé par l'e2e, pas par une revue).
- **Tokens sémantiques plutôt que rangs de palette** (Sprint 33, `app/globals.css`) — un composant
  demande `text-ink-subtle`, pas `slate-500`, donc une correction de contraste se fait **en un
  endroit**. ⚠️ `slate-400` (#94a3b8, **2,9:1** sur blanc) est **absent du vocabulaire**, pas
  seulement déconseillé : il ne doit pas revenir pour du texte porteur de sens. `--text-xs` est relevé
  à **13 px** — plancher de lisibilité des lignes qui portent les réserves.
- **Synthèse rédigée, deux règles non négociables** (Sprint 34, `lib/synthese.ts`, jumelle de
  `lib/executive.ts`) — (1) un fait absent ⇒ **phrase absente**, jamais « donnée indisponible » ;
  (2) la dernière ligne énumère toujours les manques, « comptés comme non estimés, **jamais comme
  l'absence de risque** ». ⚠️ Sur un site unique la règle 2 compte **plus** que sur un parc : il n'y a
  pas d'autre site pour relativiser un trou.
- **⚠️ Une source EN ATTENTE n'est ni un fait ni un manque** (Sprint 35, champ `enAttente` de
  `SyntheseInput`) — `undefined` signifiait « la réponse a dit non » **et** « la réponse n'est pas
  arrivée ». La synthèse affirmait donc, trois secondes après l'ouverture, que la projection 2050
  n'était pas disponible, puis se dédisait. **Même règle que le sprint 32 sur la carte** (« service
  injoignable ≠ station muette »). Exception délibérée : le **volume prélevé** n'est jamais masqué,
  car il ne dépend d'aucune requête — le masquer le ferait apparaître à la toute fin.
- **Le bandeau de sources compte les promesses RÉGLÉES, pas réussies** (Sprint 35,
  `components/SourceProgress.tsx`) — sinon un site sans station voisine resterait à 5/7 pour
  toujours, et une barre qui n'atteint jamais 100 % apprend à l'ignorer.
- **Combobox ARIA sur le champ d'adresse** (Sprint 36, `components/AddressAutocomplete.tsx`) — c'est
  **le** contrôle sans lequel aucune page ne produit rien, et il était inutilisable au clavier
  (Entrée ne faisait rien). ⚠️ `aria-activedescendant` annonce l'option **sans déplacer le focus** :
  c'est ce qui permet de continuer à taper. ⚠️ ArrowDown **rouvre** une liste fermée par Échap, sinon
  c'est un cul-de-sac qui n'existe pas à la souris. ⚠️ Les suggestions sont des `option`, **plus des
  `button`** — tout test qui les cliquait doit être recâblé.
- **Registre typé des sections de méthodologie** (Sprint 37, `lib/methodologie.ts`) — `MethodoId` est
  une union littérale dérivée par `as const satisfies`, donc une faute de frappe dans une ancre **ne
  compile pas**. ⚠️ Écrite à la main, la même faute donne un lien qui « fonctionne » et ne va nulle
  part : **le navigateur ne se plaint jamais d'une ancre absente**. ⚠️ Le titre affiché vient du
  registre, pas du point d'appel — sinon le registre devient un double à maintenir. ⚠️ Les `id` sont
  désormais un **contrat externe** (ils apparaissent dans des URL collables) : les renommer casse des
  liens sans que rien ne le signale.
- **⚠️ Un enfant de grille a `min-width: auto`** et refuse de rétrécir sous son contenu — cause des
  145 px de défilement latéral du sprint 34, et invisible sur une capture. Le détecteur tient en une
  ligne : `document.documentElement.scrollWidth - document.documentElement.clientWidth`.
- **Toutes les APIs externes passent par des routes serveur** (`app/api/*`) : pas de CORS, gestion d'erreur centralisée, cache `next: { revalidate }`. Les erreurs upstream retournent des messages français exploitables par l'UI (jamais de crash).
- **Sources** : VigiEau (`/api/zones`, 404 = non couvert, 409 = commune multi-zones — on envoie toujours lon/lat), BAN `data.geopf.fr/geocodage` (**l'ancien api-adresse.data.gouv.fr est mort**), Hub'Eau hydrométrie/piézométrie (~20 req/s fair-use, rayon 60 km, sondage parallèle de 8 candidates max), CSV arrêtés data.gouv (historique), Explore2 TRACC (projections).
- **Score composite** (`lib/score.ts`) : réglementaire 40 % + fréquence structurelle 25 % + Onde 10 % + tendance débit 12,5 % + tendance nappe 12,5 %, **renormalisé sur les composantes disponibles**. Une composante inconnue = `undefined` (jamais 0 par défaut — cf. « VigiEau down ⇒ historique inconnu, pas 0 j »). La composante fréquence utilise la **moyenne structurelle multi-années** (`joursAlertePlusMoyen`) quand des années complètes existent, sinon le cumul de l'année en cours. Reste à venir (`UPCOMING_COMPONENTS`) : IPS nappes, débits vs VCN10/QMNA5, BNPE.
- **Classification du risque** (Sprint 12, `lib/score.ts`) : le score 0-100 est traduit en 6 classes nommées (Négligeable / Faible / Modéré / Élevé / Très élevé / Critique), alignées sur la terminologie WRI/CDP. Un **indicateur de confiance** (haute/moyenne/faible) agrège couverture des composantes, distance de la station rattachée et fraîcheur des données.
- **Calendrier saisonnier** (Sprint 12) : répartition mensuelle des jours en alerte+ sur les années complètes. Source : `parMois` dans `ZoneHistory` (agrégation mensuelle ajoutée à `lib/history.ts`). Visualisé dans `RestrictionHistory.tsx` via `computeSeasonalProfile` (`lib/score.ts`).
- **Évolution du risque** (Sprint 12) : courbe de la composante « fréquence » année par année (sparkline SVG). Détecte les tendances d'aggravation/amélioration.
- **Seuils de projection** (Sprint 12) : le bloc 2050 croise le Δ VCN10 projeté avec la fréquence structurelle pour qualifier la tension future (bloc `ThresholdInsight` dans `Projection2050.tsx`).
- **Secteur d'activité = contrôle unique** (Sprint 13, fusionné avec le profil au post-Sprint 17, `lib/secteur.ts`) : 6 secteurs (agriculture/industrie/énergie/services/collectivité/autre) avec impact opérationnel détaillé par niveau de gravité (`SectorImpactPanel.tsx`). ⚠️ **Il n'y a plus qu'un seul menu déroulant** (dans `AddressSearch.tsx`) : le secteur remplace l'ancien sélecteur de « profil ». Le **profil VigiEau est dérivé** du secteur via `profilForSecteur` (agriculture→exploitation, collectivité→collectivité, industrie/énergie/services/autre→entreprise). Le secteur pilote donc à la fois la requête VigiEau (restrictions applicables) ET l'interprétation d'impact — **sans double comptage** (le secteur n'entre jamais dans `computeScore`). `SavedSite` garde `profil` (dérivé, pour la requête + rétro-compat) + `secteur`. Rétro-compat : liens/sites hérités sans secteur → `secteurForProfil` infère. **« Particulier » (usage domestique) est conservé mais marqué secondaire** (`domestic: true` dans `SECTEURS`) : présenté dans un `<optgroup>` distinct de `AddressSearch`, avec un encart dans `SectorImpactPanel` précisant que l'outil vise les sites professionnels. Il fonctionne comme les autres (profil VigiEau `particulier` + table d'impacts domestiques). Test : `npx tsx scripts/test/secteur.test.ts`.
- **Synthèse portefeuille** (Sprint 13) : le dashboard affiche des indicateurs agrégés (nombre de sites, score moyen, score max, répartition par classe de risque). Chaque site porte un badge de classe de risque WRI/CDP à côté du score. L'export CSV inclut le secteur et la classe de risque.
- **Lien de partage** (Sprint 14) : bouton « 🔗 Partager » qui copie une URL encodant l'analyse complète (lat/lon/label/profil/**secteur**). `buildParams`/`parseInitialParams` dans `HomeClient.tsx` gèrent l'aller-retour ; le secteur est restauré à l'ouverture d'un lien. Zéro serveur — tout tient dans l'URL, cohérent avec le local-only.
- **Mode hors-ligne (PWA)** (Sprint 14) : `public/manifest.webmanifest` + service worker `public/sw.js` enregistré en prod uniquement (`ServiceWorkerRegister.tsx`, monté dans `app/layout.tsx`). Stratégie : network-first pour les navigations, stale-while-revalidate pour les assets Next, **`/api/*` jamais mis en cache** (données live, egress requis, pas de risque périmé servi comme actuel). Le shell + le dashboard localStorage marchent hors-ligne ; les données temps réel restent « indisponibles ». ⚠️ Le SW ne s'enregistre pas en dev (conflit Turbopack). Pour tester : `npm run build && npx next start`, puis DevTools → Application → Service Workers.
- **Benchmark projection** (Sprint 15) : le bloc 2050 situe la baisse d'étiage projetée du site (médiane VCN10 à +2,7 °C) dans la distribution nationale (34 418 communes) + départementale. Distribution de référence pré-calculée **hors-ligne** depuis les shards Explore2 déjà embarqués par `scripts/projections/build_benchmark.py` → `data/projections/benchmark.json` (58 Ko, 101 breakpoints de percentile par scope). Loader `benchmarkForCommune` (`lib/projections.ts`), helper pur `severityPercentile` (`lib/projectionsShared.ts`, testé), remonté dans `ProjectionPayload.benchmark`, affiché par `BenchmarkInsight` (`Projection2050.tsx`). ⚠️ Si les shards Explore2 changent, **régénérer** le benchmark (le script relit les shards locaux, aucun egress). Test : `npx tsx scripts/test/benchmark.test.ts`.
- **Répartition géographique du portefeuille** (Sprint 16) : le dashboard regroupe les sites par département (`PortfolioByDepartment.tsx`) avec nombre de sites + score moyen, classés du risque le plus élevé au plus faible (effet « heatmap » par barre colorée). Département déduit du `citycode` via `departementCode` (`lib/departements.ts`, référentiel statique embarqué : 96 métro + Corse 2A/2B + DOM 971-976), testé (`scripts/test/departements.test.ts`). Ne s'affiche que si les sites couvrent ≥2 départements. ⚠️ Pas de vraie choroplèthe départementale (géométries non embarquées, egress bloqué) — c'est une agrégation, pas une carte polygonale ; noté en backlog.
- **Rapport ESG / ESRS E3** (Sprint 17, `lib/report.ts`) : builder pur `buildMarkdownReport(input)` → Markdown structuré (identification, score + décomposition, statut réglementaire, historique, projection 2050 + benchmark, correspondance ESRS E3/TNFD/CDP, sources + avertissement). Assemble les données déjà à l'écran ; le bouton « 📄 Rapport ESG » (`HomeClient.tsx`) fetch la projection à la volée puis télécharge un `.md`. 100 % navigateur, aucune donnée serveur. `reportFilename` slugifie le libellé. Testé (`scripts/test/report.test.ts`). C'est un **support de contexte**, pas une déclaration de conformité (avertissement explicite dans le rapport).
- **Carte choroplèthe départementale** (Sprint 19-B) : sur le dashboard, `PortfolioChoropleth.tsx` (MapLibre, fond neutre sans tuiles externes) teinte chaque département selon le score moyen des sites qu'il contient. Polygones départementaux simplifiés récupérés **via GitHub Actions** (egress bloqué en dev — voir workflow `fetch-refdata.yml` + script `scripts/refdata/fetch_refdata.py`), coords arrondies à ~100 m, servis par `/api/departements` (`data/refdata/departements.geojson`, 226 Ko, 96 départements). Inclus dans `outputFileTracingIncludes`.
- **Indice d'anticipation** (Sprint 20, `lib/anticipation.ts` + `AnticipationPanel.tsx`) : comble l'**horizon temporel manquant** entre le statut VigiEau *maintenant* et la projection *2050* — les prochaines semaines jusqu'à la fin de l'étiage, dont une entreprise a besoin pour **anticiper** un passage/aggravation en restriction. Fonction pure `computeAnticipation(input)` : deux volets. (1) **Base saisonnière = l'ancre** — pic du risque mensuel historique (`computeSeasonalProfile`, réutilisé, désormais avec un `currentYear` injectable pour les tests) sur les mois de l'horizon (mois courant + 2) ; hors saison, gate l'indice bas quel que soit l'état physique (les arrêtés sécheresse sont saisonniers). (2) **Pression état actuel** — signaux précurseurs déjà calculés, normalisés 0-100 : IPS nappe (poids le plus fort — signal le plus lent/prédictif), débit VCN10/QMNA5, assecs Onde, niveau VigiEau courant, chaque signal physique nuancé par sa tendance 14 j ; ce volet ne relève l'indice que si la saison est « ouverte » (`openness = seasonalBase/40`). Combinaison `seasonalBase*0.5 + stateScore*0.5*openness`, puis **facteur de trajectoire** (cumul jours alerte+ de l'année en cours vs moyenne au même stade → ×0.85-1.20) et **plancher** si déjà en restriction (alerte→55, crise→75). Sortie = 4 niveaux (Peu probable/Possible/Probable/Très probable) + **moteurs détaillés** + confiance (réutilise la logique de `scoreConfidence`) + avertissement « conditions propices, pas une prévision de l'arrêté ». **100 % in-repo, aucun egress** : `AnticipationPanel` calcule à partir des props déjà en state dans `HomeClient` (histInfo/onde/indicators) — dégradation gracieuse tant que les signaux Hub'Eau ne sont pas arrivés. Placé entre l'historique et le bloc 2050. Test : `npx tsx scripts/test/anticipation.test.ts`. **Validé sur données réelles le 2026-07-30** (§ Issues connues, mode diag `anticipation`) : résultat non dégradé et cohérent sur un site réel actuellement sous restriction.
- **Prévision de nappe MétéEAU : lien sortant, PAS d'intégration** (post-Sprint 20, `lib/meteeau.ts`) — ⚠️ **tranché, ne pas re-prober.** Le follow-up « prévision nappe » a été instruit en 5 passes Actions. Constat : l'API **MétéEAU des nappes** (`api.meteeaunappes.brgm.fr`) existe et conviendrait parfaitement (prévision IPS 6 mois par `code_bss`, seuils saisonniers) mais elle est **verrouillée par OAuth2** — Keycloak BRGM, `security` global sur *toutes* les opérations, 401 `WWW-Authenticate: Bearer` — et **aucun dataset de prévision n'est publié sur data.gouv**. Ré-héberger serait en outre non viable (couverture nationale, péremption mensuelle) et juridiquement incertain. **Décision utilisateur : renvoyer par un lien.** `AnticipationPanel` affiche donc, **dans les deux états** (indice disponible ou non), un lien vers le visualiseur public + un encart expliquant *pourquoi* c'est un lien (`METEEAU_WHY_LINK`) — exigence explicite : l'utilisateur doit le comprendre sans aller dans la méthodologie. ⚠️ **L'URL publique n'est pas `meteeaunappes.brgm.fr`** (site Drupal institutionnel) mais **`https://app.meteeaunappes.brgm.fr/`** (SPA Angular, le vrai visualiseur) — trouvé en crawlant les liens *hors-domaine* du Drupal. Pas de schéma de centrage lat/lon confirmé (routes Angular dans les bundles JS, non inspectées) → `METEEAU_SUPPORTS_CENTERING = false`, on n'invente pas de paramètres que le visualiseur ignorerait ; passer le flag à `true` suffirait si une route de centrage était confirmée un jour. **La prévision n'entre pas dans le score** : la dimension nappe de l'indice reste l'IPS *observé* (Hub'Eau/ADES ouvert). Le scaffolding de probe (`scripts/nappe/`, workflow, `data/refdata/nappe-*.json`) a été **purgé** après analyse (convention « diag à purger ») — récupérable dans l'historique git (commits `67ff056`→`0c82bdb`).
- **Jours d'activité contrainte** (Sprint 21, `lib/interruption.ts` + `lib/restrictions.ts` + `InterruptionPanel.tsx`) : la **synthèse** qui fait enfin parler entre eux les arrêtés, l'anticipation et la projection. `jours contraints = Σ jours(niveau) × exposition(niveau)` — pondération **bornée**, jamais un quotient (la forme `probabilité × durée / priorité` initialement envisagée donnait 180 j d'interruption dans une sécheresse de 90 j). Trois horizons : *année type* (moyenne des **années complètes uniquement** — `parAnnee` contient aussi l'année en cours partielle, cf. `lib/history.ts:313-319`), *fin de saison* (climatologie mensuelle × ajustement dérivé de `AnticipationResult.index`, **consommé** et non redupliqué), *2050* (`dtBE_yr` allonge en jours, `VCN10_ete` intensifie en décalant les jours vers le niveau supérieur **en cascade descendante, total conservé**). Test : `npx tsx scripts/test/interruption.test.ts`.
- **Périodes RLE : le calendrier des arrêtés, récupéré et non recalculé** (Sprint 26, `lib/history.ts`) — `ZoneHistory.periodes` = triplets **plats** `[indexJour, longueur, rang]`. Le parseur construisait déjà une map jour→rang par zone et **la jetait** après agrégation ; les agrégats répondent « combien de jours », jamais « les mêmes jours ? » ni « quelle durée d'épisode ». ⚠️ **Émises uniquement sur `?periodes=1`** — sans le paramètre la réponse est strictement celle d'avant (la fiche site n'en a pas besoin et serait alourdie pour rien). RLE parce qu'un arrêté est un intervalle : quelques dizaines de runs par zone et par décennie contre 3 650 entrées brutes. ⚠️ **Construction par balayage de la plage de jours, PAS par tri des clés** : sur ~2 200 zones un tri par comparateur coûtait ~+1 400 ms, le balayage **+330 ms mesurés au banc** (2 300 → 2 630 ms, `scripts/test/history-window.bench.ts`). Un `WeakMap` évite de recompresser les alias (chaque zone est indexée sous son code *et* son id numérique, sur la même map). ⚠️ Un run casse aussi sur un **trou de calendrier**, pas seulement sur un changement de niveau — sinon deux arrêtés distants de quinze jours deviendraient une restriction continue et effaceraient une autonomie de stockage qui a réellement eu le temps de se reconstituer. Invariant testé : les jours reconstitués depuis les périodes **égalent exactement** `parAnnee[*].joursParNiveau`.
- **Le portefeuille est un objet d'analyse, pas une somme** (Sprint 26, `lib/portefeuille.ts` + `PortfolioCorrelation.tsx`) — **le cœur du sprint.** 20 sites sur 18 zones et 20 sites sur 3 zones donnaient le *même* total de jours contraints : c'est faux, dans le second cas un seul arrêté immobilise les trois quarts du parc **le même jour**. `computePortfolio` (pur, hors ligne, comme `computeAnticipation`/`computeInterruption`) rejoue les années complètes jour par jour et sort : **pic de simultanéité daté avec sa durée** (membres relus sur la période affichée, pas sur le premier jour à atteindre le pic), distribution « k sites simultanés × jours/an », année la plus lourde, **pic pondéré** (Σ exposition × dépendance = « équivalent sites à l'arrêt »), **HHI** restitué par son inverse lisible (« vos 40 sites se comportent comme 4,2 zones indépendantes »), **grappes co-exposées** (zone avant bassin : une zone partagée est un *arrêté* partagé, un bassin partagé n'est qu'une hydrologie partagée), et **part des jours partagés** par site (diversifiant vs aggravant). La littérature nomme le phénomène *drought synchronicity* et relève que les évaluations de risque « supposent l'indépendance entre localisations » — ici rien n'est supposé, dix ans d'arrêtés sont publiés. ⚠️ **Un seul appel `/api/history&periodes=1`** sur l'union des zones du parc (la route accepte déjà 100 codes), quel que soit le nombre de sites. ⚠️ Le rejeu **exclut l'année en cours** (partielle) et compte les années calmes intermédiaires **comme des zéros mesurés**, pas comme des trous. Test : `npx tsx scripts/test/portefeuille.test.ts`.
- **Ressource renouvelable par site** (Sprint 27, `lib/ressource.ts` + `RessourcePanel.tsx`) — **lève le blocage « BNPE dans le score : bloqué sur les données de ressource renouvelable par sous-bassin » porté depuis le Sprint 10.** Le blocage tenait à chercher un *jeu de données tout fait* ; la ressource **se calcule** depuis des séries déjà téléchargées. `computeModule` (`lib/hubeau.ts`) prend la **moyenne de la même série QmnJ 18 ans** qui sert déjà au VCN10 — aucun téléchargement en plus — soit le **module**, mesure standard de la ressource renouvelable de surface. `module ÷ surface_bv` = **débit spécifique** (l/s/km²), transposé à la surface de la commune → ressource m³/an ; ÷ prélèvements BNPE (déjà récupérés) = **taux d'exploitation** sur l'**échelle WRI Aqueduct** (réutilisée, pas réinventée : c'est le langage des lecteurs ESG) ; ÷ volume déclaré (Sprint 26) = part du site. La transposition par débit spécifique est la **méthode de référence pour un bassin non jaugé** (OFB, guides DREAL), pas une invention. **Hors score composite** (décision utilisateur) — même règle de non-double-comptage que `secteur`/`origine`/`dependance`. Test : `npx tsx scripts/test/ressource.test.ts`.
- **⚠️ `surface_bv` est sur `referentiel/sites`, PAS sur `referentiel/stations`, et manque une fois sur deux** (Sprint 27, mesuré passes 26-28) — `stations_has_surface_bv: false`, `sites_has_surface_bv: true`. La station porte `code_site`, donc **une jointure supplémentaire** (`siteCatchment`) ferme la boucle. ⚠️ **Mesuré sur 2 000 sites : 895 seulement portent une surface (45 %)** — le modèle est donc muet sur plus de la moitié du réseau, et ce n'est pas un bug. Surfaces mesurées : min 0,001 km², médiane 173, q75 732, max 65 300 (la Loire). C'est cette dispersion qui impose une **borne au rapport de surfaces** : transposer la Loire à une commune de 20 km² produit un **refus explicite**, pas un chiffre.
- **⚠️ `influence_generale_site` est affiché, jamais calculé avec** (Sprint 27) — le référentiel des sites porte ce champ (barrage, prélèvements amont : le module ne reflète alors pas un régime naturel), mais **la nomenclature Sandre n'a pas pu être lue** (l'API référentiel renvoie 400 sur deux formes d'URL essayées). Inventer une échelle de sévérité serait exactement l'erreur que le dépôt s'interdit. Le code est donc **remonté brut**, déclenche une réserve dès qu'il est non nul, et **ne déplace aucun chiffre** (testé).
- **Piste repérée, non exploitée : `sa:BVSpeMasseDEauSurface_VEDL2019_FXX`** (Sprint 27) — parmi les 699 couches du WFS Sandre figure le **bassin versant spécifique de chaque masse d'eau de surface**. C'est le **vrai polygone hydrographique**, là où `lib/ressource.ts` transpose aujourd'hui sur l'emprise **communale** faute de mieux. Le remplacer lèverait d'un coup la réserve « commune ≠ bassin versant », qui est la principale approximation du modèle. Non instruit : ni la volumétrie ni les attributs n'ont été vérifiés. **À sonder avant d'y investir**, comme le reste.
- **⚠️ Aucun état quantitatif national des masses d'eau en open data — instruit, ne pas re-prober.** Le volet « officiel » qui devait couvrir les sites sur forage **n'existe pas sous forme exploitable**. Mesuré : le WFS Sandre expose **699 couches**, dont `sa:MasseDEauSouterraine_VRAP2022_FXX` (national, version SDAGE 2022-2027) — mais ses **18 attributs sont du référentiel pur** (`CdMasseDEau`, `NomMasseDEau`, `SurfaceTotaleKm`, `Karstique`, `MultiCouches`, `NatureEcoulement`…) et **aucun ne porte d'état ni d'objectif quantitatif**. L'état des lieux existe bien, mais **fragmenté par agence et par région** (AEAG en CSV, DREAL Occitanie en zip, Normandie en shapefile, Loire-Bretagne à part), en formats hétérogènes — un chantier à part entière, pas un référentiel à embarquer. **Conséquence assumée** : un site en origine `souterrain` reçoit un **refus explicite** disant qu'aucun jeu national n'existe, et se voit renvoyé vers l'**IPS piézométrique déjà calculé**, qui décrit l'état de *sa* nappe. Aucun volume souterrain n'est inventé.
- **⚠️ DEUX dénominateurs, deux questions — ne jamais les confondre** (Sprint 28, `lib/ressource.ts`). Le Sprint 27 divisait les prélèvements par la **production locale** tout en appelant le résultat « taux d'exploitation » et en le graduant sur l'**échelle WRI**, qui rapporte au contraire les prélèvements à la ressource **disponible, apports amont compris**. Le nom et l'échelle désignaient une grandeur, le calcul en faisait une autre. Le modèle sépare désormais :
  - **`pressionCoursEau`** = prélèvements ÷ **débit disponible au point** (`module × secondes/an`, le module intégrant tout l'amont). « Le cours d'eau a-t-il assez d'eau ? » — ⚠️ **seule** à porter la classe WRI.
  - **`autonomieTerritoire`** = prélèvements ÷ **production du territoire**. « Ce territoire vit-il de sa propre eau ? » — ⚠️ **jamais de classe WRI**, un test l'interdit explicitement. Au-delà de 1, `dependanceAmont` : la commune vit d'une eau produite en amont, situation ordinaire d'une ville de grand fleuve.
  - **Mesuré sur Chartres** (rejeu réel) : les **mêmes entrées** donnent **0,83 % « Faible »** en pression et **37 %** en autonomie. Deux ordres de grandeur — aucun nombre unique ne pouvait porter les deux.
  - ⚠️ **Le garde-fou du Sprint 27 traitait le symptôme** : il retirait la classe au-delà de 100 %, là où le résultat crevait les yeux (Toulouse à 487 %). Le défaut existait à tous les niveaux, invisible en dessous parce qu'un chiffre entre 0 et 100 % « a l'air » d'un taux d'exploitation — Chartres à 37 % « Élevé » ne choquait personne et était tout aussi faux.
- **⚠️ La pression ne demande QUE le module — gain de couverture majeur** (Sprint 28) : pas de `surface_bv`, absent sur **55 % du réseau** et qui faisait auparavant échouer **tout** le panneau. Les refus ne condamnent désormais **que leur branche** : mesuré sur Orléans, le rapport de surfaces aberrant (36 970 km² contre 28) annule la production locale et **laisse la pression debout** (0,03 % sur la Loire, là où le modèle refusait tout).
- **⚠️ La station rattachée n'est pas forcément la source du site** (Sprint 28) — mesuré : **Toulouse est rattachée à l'Hers** (768 km²) alors que la ville prélève dans la **Garonne**, d'où une pression de **75 % « Très élevé » sur la mauvaise rivière**. Réserve affichée dès qu'une pression existe ; et quand **les deux signaux concordent** (`dependanceAmont` + pression > 40 %), une réserve dédiée dit que la commune est presque certainement alimentée par un cours d'eau plus important que celui mesuré. ⚠️ Cela **signale** l'artefact sans le corriger — le chiffre reste affiché avec sa classe.
- **Invariant de rejeu : `pression < autonomie`** (Sprint 28, `scripts/diag/replay-ressource.ts`) — la surface d'une commune est une fraction du bassin qui l'alimente, donc la pression sur le cours d'eau doit **toujours** ressortir inférieure au ratio d'autonomie. L'inverse signalerait que les **deux divisions ont été échangées** — le genre d'erreur qu'aucun test unitaire sur nombres inventés ne peut attraper, puisque les deux formules resteraient individuellement correctes. ⚠️ L'invariant suppose la commune plus petite que le bassin de la station : faux en principe pour une très grande commune sur un tout petit bassin, où il alerterait à tort.
- **⚠️ La ressource renouvelable N'EST PAS un volume prélevable** (Sprint 27) — une part doit rester au milieu (débit réservé, DOE), et l'outil n'a pas la donnée DOE par point nodal. `RESSOURCE_RESERVES.pasUnDroit` est affichée **inconditionnellement**, hors de tout repli dépliable : le mésusage le plus probable de ce panneau est de lire la ressource comme un droit de prélèvement. Autres réserves systématiques : transposition spatiale, module court (années récentes plus sèches ⇒ module sous-estimé ⇒ taux **surestimé**), commune ≠ bassin versant.
- **⚠️ Le dénominateur d'un rejeu est la COUVERTURE DU FICHIER, jamais le premier arrêté** (Sprint 26, `PortfolioInput.couvertureDepuis`) — **bug attrapé uniquement sur données réelles, invisible sur fixtures.** VigiEau **redécoupe son référentiel de zones** : un code en vigueur aujourd'hui n'apparaît tout simplement pas dans les arrêtés antérieurs à sa création. Mesuré au run 24 : le fichier couvre **2017→2026**, `anneesCompletes = 9`, mais `84_69_0004` (Lyon) ne commence qu'en **2022**, `76_66_7` (Perpignan) et `24_028_0003` (Chartres) en **2023**. Dater la fenêtre du premier arrêté divisait donc les grandeurs « par an » par 4 au lieu de 9 : **59 j/an de jours multi-sites au lieu de 26,2**. Une année couverte sans arrêté est un **calme mesuré**, pas un trou — exactement la règle que `lib/history.ts` applique déjà via son `fileMinYear`. La couverture vient de `diag.coverage.from` ; une couverture annoncée *après* les données est ignorée plutôt que de tronquer le rejeu. Le même dénominateur sert aux jours d'arrêt nets d'autonomie.
- **⚠️ « Pondérer par les volumes » n'était pas bloqué — la donnée n'appartient simplement pas à VigiEau** (Sprint 26). Ce HANDBOOK portait « bloqué : VigiEau ne publie aucun volume par usage ». Vrai de la source publique, et hors sujet : **l'entreprise connaît ses propres volumes**. Quatre champs déclarés par site (`DonneesInternes` dans `lib/sites.ts` : `volumeM3`, `autonomieJours`, `coutJourEuros`, `caAnnuelEuros`), bloc `<details>` repliable dans `AddressSearch` pour ne pas alourdir la découverte. `m³ à risque = volume × joursContraints / 365` (prélèvement moyen journalier, écrit comme tel). `€ = coutJourEuros × jours`, **repli** `CA × 0,005 × jours` (ordre de grandeur Swiss Re : un jour d'interruption ≈ 0,5 % du CA annuel) **toujours étiqueté `eurosSource: "repli_ca"`** et signalé dans l'UI. ⚠️ **Aucune de ces grandeurs n'entre dans `computeScore`** — même règle de non-double-comptage que `secteur`/`origine`/`dependance` : elles *traduisent* le risque, elles ne le mesurent pas. ⚠️ Un champ vidé vaut `undefined`, **jamais 0** : le site est compté *non estimé*, il ne déclare pas un prélèvement nul. ⚠️ `m3Declares` (volumes saisis) est distinct de `m3Sites` (volumes convertis) : un site avec volume mais sans historique manque de **jours**, pas de volume — le confondre enverrait son propriétaire remplir un champ déjà rempli.
- **Jours d'arrêt nets d'autonomie** (Sprint 26) : `Σ_épisode max(0, durée − autonomieJours)`. **Impossible sans les périodes** — un tampon de 3 j absorbe une restriction de 2 j, et aucun total annuel ne peut le voir. ⚠️ Deux runs **adjacents** de niveaux différents forment **un seul** épisode : une alerte qui durcit en crise n'a jamais laissé la bâche se remplir.
- **Executive summary : des faits, pas un gabarit** (Sprint 26, `lib/executive.ts` + `PortfolioExecutiveSummary.tsx`) — en tête de `/sites`, **après l'entrée des sites et avant les tuiles KPI** : la synthèse d'abord, les faits ensuite. Six lignes dans l'ordre de lecture d'un comité : situation · coût récurrent · concentration · trajectoire 2050 · où agir (Pareto : plus petit ensemble de sites portant la moitié des jours) · **ce que le résumé ne sait pas**. ⚠️ **Deux règles non négociables** : (1) chaque phrase naît d'un fait calculé — fait absent, **phrase absente**, jamais « donnée indisponible » (un résumé rempli de trous apprend au lecteur à le sauter) ; (2) la dernière ligne **énumère toujours** ce qui manque et conclut que ces sites sont « comptés comme non estimés, **jamais comme des sites sans risque** » — à l'échelle d'un parc, un manque tu se lit comme un zéro. Le **même builder** alimente l'écran et le rapport ESG portefeuille, donc le PDF et le tableau de bord ne peuvent pas diverger. Test : `npx tsx scripts/test/executive.test.ts`.
- **⚠️ L'exposition est LUE dans les arrêtés, jamais posée** (`lib/restrictions.ts`) — c'est le cœur du Sprint 21 et une correction explicite de l'utilisateur (« ne pas inventer de coefficients »). La ressource « Restrictions » **n'a pas de champ de sévérité structuré** : `usage.u.description` est de la prose préfectorale. Mais elle est régulière et souvent **chiffrée** : « Interdiction de 8h à 20h » = 12 h/24 = 0,5, mesuré ; les pourcentages sont parsés. À défaut de quantité : interdiction 1,0 · interdiction avec dérogation (« sauf ») 0,85 · sensibilisation 0. **Illisible ⇒ `undefined`, jamais 0.** Exposition = **moyenne** sur les usages dont le drapeau `concerne_<profil>` est vrai — *pas* un max : un usage interdit sur quinze n'arrête pas un site. Tests calibrés sur des libellés **verbatim** du fichier (`scripts/test/restrictions.test.ts`). ⚠️ Ordre des tests important : `AWARENESS` avant `NO_LIMIT` (des mesures disent les deux), et les formes chiffrées avant les formes grossières.
- **Origine de l'eau** (Sprint 21, `levelForOrigin` dans `lib/vigieau.ts`) : VigiEau publie un niveau **par type de zone** (SUP/SOU/AEP) et `worstLevel` prend le **max** — prudent pour le score, faux pour l'impact opérationnel (un site sur réseau AEP héritait de la gravité d'une nappe qu'il ne pompe pas). `levelForOrigin` vit **à côté** ; `worstLevel` est **inchangé** pour ne pas déplacer le score des sites déjà enregistrés. Défaut `inconnu` = comportement historique.
- **Données restrictions embarquées** (`data/restrictions/`, construites par `scripts/restrictions/build_restrictions.py` via Actions) : `guide.json` = **matrice nationale officielle** « Restriction Guide Sécheresse » (20 usages × 4 niveaux × 4 audiences, 19 Ko) servant de **repli** ; `zones/<dep>.json` = usages restreints publiés par département (99 shards, 7,6 Mo, plus gros 273 Ko). Servis par `/api/restrictions?dep=&type=&profil=` → exposition par niveau + détail auditable. **Aucun egress à l'exécution** (la source fait 23 Mo). ⚠️ **Le classifieur de sévérité n'est PAS réimplémenté en Python** : le script ne fait que réduire la source aux tuples distincts, le classifieur TS unique les score — sinon les deux dérivent en silence. `outputFileTracingIncludes` mis à jour.
- **BNPE : le milieu EST récupérable** (Sprint 21, `lib/bnpe.ts`) — ⚠️ **corrige une note erronée de ce HANDBOOK.** « La chronique BNPE n'a pas de champ milieu » est vrai *de la chronique*, mais `referentiel/ouvrages` porte `code_type_milieu` / `libelle_type_milieu` (+ `code_bdlisa`) et se joint sur `code_ouvrage`. Probe mesuré : **taux de jointure 1,0** (Chartres : eau potable = superficiel, industrie = souterrain). `aggregateBnpe(rows, milieuByOuvrage?)` produit désormais `parUsage[].parMilieu` et `parMilieu`. Un ouvrage non joint reste en `inconnu` (jamais écarté) ; si **aucun** ne se joint, `milieuAvailable: false` plutôt qu'un seau unique présenté comme une répartition.
- **Arbitrage des usages** (Sprint 21, `lib/arbitrage.ts` + `BnpePanel`) : hiérarchie du **décret n° 2021-795** (usages d'agrément restreints en premier → eau potable/santé/sécurité maintenues en dernier), secteur du site mis en évidence. **Purement descriptif** — aucun chiffre du produit n'en dérive. Répond à la question « une entreprise en crise ne peut-elle plus prélever ? » : non, seuls les prélèvements *non prioritaires* cessent.
- **ZRE hors métropole : instruit, aucune source — ⚠️ ne pas re-prober.** Le WFS Sandre annonce `sa:ZRE` **à côté** de `sa:ZRE_FXX`. `sa:ZRE` se lit correctement (en `OUTPUTFORMAT=geojson` ; `application/json` renvoie 400), mais il produit **exactement le même contenu** : reconstruction identique à **13 033 communes sur 64 départements**, Corse et outre-mer vides. Aucune couche ZRE plus large n'est exposée par ce service. On ne peut pas distinguer d'ici « ces territoires n'ont pas de ZRE » de « la couche les omet » — d'où le maintien de `available: false` pour 2A/2B/97x/98x dans `/api/transition`, qui est la lecture prudente et correcte.
- **Explore2 : QMNA5 et recharge de nappe n'existent PAS dans cette collection — ⚠️ ne pas re-chercher.** L'énumération complète des ressources des deux jeux TRACC (agrégé et détaillé) ne contient que **quatre indicateurs** : `VCN10_été`, `QA_yr`, `dtBE_yr`, et `QJXA_yr`/`dtCrues_yr`. Les trois premiers sont déjà extraits ; les deux derniers sont des indicateurs de **crue**, hors sujet pour un produit de pénurie. La recharge des nappes relèverait du volet souterrain DRIAS-Eau / Aqui-FR, **un autre jeu de données** — la projection des zones SOU continue donc de s'appuyer sur un indicateur de débit de surface, limite à assumer telle quelle.
- **Humidité des sols (SWI)** (Sprint 23, `lib/swi.ts` + `/api/swi` + `scripts/swi/build_swi.py`) : précurseur le plus **précoce** de la chaîne (le sol s'assèche des semaines avant la nappe), standardisé comme l'IPS — rang de la valeur du mois dans la distribution de la **même maille SAFRAN** pour le **même mois calendaire** sur 1990-2019. ⚠️ **Séparation embarqué / live délibérée**, sur le précédent MétéEAU : la climatologie (8 981 mailles × 12 mois → `data/swi/clim/*.json`, 3,9 Mo en 40 buckets, + `cells.json` 340 Ko) est stable et embarquée ; le **mois courant n'est jamais embarqué** (il se périmerait en silence) et se récupère à la volée depuis le fichier décennal `swi.202001-202512.csv` (~22 Mo, cache 24 h), même schéma que le CSV des arrêtés. Poids dans `computeAnticipation` : **12**, sous la nappe (30) — signal plus rapide donc plus bruité. Tests : `npx tsx scripts/test/swi.test.ts`.
- **⚠️ SWI : la publication accuse un retard de plusieurs mois.** Mesuré en prod le 2026-07-31 : la donnée la plus récente du fichier décennal était **décembre 2025** (7 mois). Un précurseur censé éclairer les *prochaines semaines* ne peut pas être alimenté par ça — et `swiReading` étiquette la valeur avec **son propre mois**, donc l'UI aurait annoncé « décembre » à quelqu'un lisant en juillet. Garde-fou : `MAX_AGE_MONTHS = 3`, au-delà la lecture est marquée `stale`, **affichée avec sa date mais exclue de l'indice** (qui se renormalise). Ne pas retirer ce garde-fou sans vérifier que la publication a rattrapé son retard.
- **⚠️ SWI : le CRS documenté est FAUX.** Le jeu data.gouv annonce « Lambert 2 étendu, en hectomètres » ; les valeurs réelles (`LAMBX≈641374`, `LAMBY≈7106309`) ne collent qu'au **Lambert-93 en mètres (EPSG:2154)**. Le script de build ne fait donc confiance à aucune des deux : il essaie les candidats et retient celui dont les mailles converties tombent dans l'emprise France — mesuré **100 % pour Lambert-93 m, 0 % pour les trois autres** — et sort en erreur si aucun ne passe. Ne pas « corriger » vers la doc.
- **Colonnes réelles du SWI** : `NUMERO, LAMBX, LAMBY, DATE (AAAAMM), SWI_UNIF_MENS`. ⚠️ La ressource au format `csv` du jeu est une **notice**, pas des données — les données sont les fichiers `swi.<AAAAMM>-<AAAAMM>.csv` au format **`csv.gz`**. Une première passe s'y est trompée et a rapporté une ligne de commentaire comme schéma.
- **Bassin & agence de l'eau** (Sprint 24, `lib/bassins.ts` + `/api/transition` + `data/refdata/bassins-communes.json`) : **35 186 communes** rattachées aux 9 bassins DCE via `sa:BassinDCE` (Sandre WFS, 14 entités, colonne `CdBassinDCE`), jointure spatiale calculée par `scripts/refdata/fetch_bassins.py` (mode `bassins` du workflow refdata). Le dernier saut — 9 codes de bassin → 6 agences — est en dur dans `lib/bassins.ts` car il est stable ; **le rattachement commune→bassin ne l'est jamais** (les bassins suivent l'hydrologie, une table par département serait fausse à chaque ligne de partage et fausse invisiblement). ⚠️ Le bassin est résolu **avant et indépendamment du garde-fou ZRE** : les deux référentiels n'ont pas la même portée — Ajaccio résout le bassin E (Corse) alors que son statut ZRE reste indisponible. Les fusionner perdrait le bassin partout où la ZRE ne va pas. Répartition mesurée : H 8097, D 7360, G 6906, F 6757, C 2526, A 2361, B1 710, E 360, B2 109.
- **Propluvia `zones_communes.csv` : sans objet — ⚠️ ne pas l'intégrer.** L'idée était d'éviter un aller-retour VigiEau en pré-résolvant les codes de zone par commune. Mais ce fichier ne porte **que le mapping zone↔commune**, jamais le **niveau de gravité en vigueur**, qui est précisément ce qu'on va chercher chez VigiEau. L'appel resterait donc nécessaire : zéro économie, une dépendance de données en plus et un risque de péremption (le zonage évolue).
- **Tarification progressive locale : pas de référentiel national.** Elle est fixée commune par commune ou par syndicat d'eau, sans base nationale consolidée en open data. Le panneau de transition renvoie donc à l'agence de bassin (dont les redevances, elles, sont publiées) plutôt que de prétendre à une donnée locale qui n'existe pas.
- **Rattachement à l'aquifère (BDLISA)** (Sprint 25, `lib/bdlisa.ts` + `/api/bdlisa` + `piezoIndicators`) — ⚠️ **le blocage précédent était une mauvaise question, pas une donnée manquante.** Constat mesuré (`data/refdata/bdlisa-probe.json`) : la couche `sa:EntiteHydroGeol` du WFS Sandre **répond au point** (bbox ~500 m), renvoie **4-5 entités emboîtées** par point avec `CodeEH` / `LibelleEH` / `NiveauEH` / `EtatEH`, et donne des codes distincts par territoire. Le blocage documenté (« quelle entité choisir ? ») **disparaît en cessant de choisir** : on retient l'**ensemble** des entités contenant le site et on teste l'**appartenance** du `code_bdlisa` du piézomètre à cet ensemble. Ordre de tri : **disponibilité → appartenance aquifère → distance**. ⚠️ Un piézomètre **sans** code d'aquifère n'est **pas** pénalisé (absence ≠ preuve d'un autre aquifère) ; sans info BDLISA le tri redevient exactement l'ancien. Interrogé **en direct** (pas d'embarqué : jeu national volumineux, un seul point utile, cache 30 j). Ne concerne que les **nappes** — les stations de débit restent à la distance. Test : `npx tsx scripts/test/bdlisa.test.ts`.
- **Risque de transition** (Sprint 19-A, **livré**) : `TransitionRiskPanel.tsx` sur la fiche site + `lib/transition.ts` (Plan Eau 2023, trajectoire par secteur, explication ZRE — statique et exact). Statut **ZRE** (Zone de Répartition des Eaux) résolu par `/api/transition?citycode=` depuis `data/refdata/zre-communes.json` — **13 033 communes** classées, jointure spatiale calculée dans le script Actions. **Source : Sandre WFS national `sa:ZRE_FXX`** (`services.sandre.eaufrance.fr/geo/sandre?…REQUEST=GetFeature&TYPENAMES=sa:ZRE_FXX&OUTPUTFORMAT=application/json`) — trouvée via le **mode `probe`** du script (`data/refdata/probe.json` classe chaque source candidate). Les URLs data.gouv se sont révélées inexploitables (hôte INSPIRE mort ; portails HTML ; l'entrée « ZRE » d'Île-de-France = « zones de reconquête économique », rien à voir). Couverture : France métropolitaine continentale (hors Corse/DOM). `/api/transition` traite une liste vide comme « indisponible » (jamais « hors ZRE » à tort). Tests : `npx tsx scripts/test/transition.test.ts`. ⚠️ Le mode `probe` reste dans le script (`mode:"probe"` dans la requête) pour re-diagnostiquer une source si besoin.
- **Escape hatch données via Actions** (Sprint 19) : nouveau workflow `.github/workflows/fetch-refdata.yml` déclenché par un push touchant `data/refdata-request.json` (ou le script). Le runner a le réseau complet, exécute `scripts/refdata/fetch_refdata.py` (pip install requests geopandas shapely), commite les sorties dans `data/refdata/`. Manifest de provenance/erreurs dans `data/refdata/manifest.json`. Même schéma que `extract-projections.yml`. ⚠️ Les URLs data.gouv `/fr/datasets/r/<id>` renvoient souvent une **page HTML** et non le fichier ; télécharger via `requests` puis lire localement, et rejeter les réponses HTML.
- **Rapport ESG portefeuille** (Sprint 18, `lib/report.ts`) : `buildPortfolioMarkdownReport({ generatedAt, sites })` → Markdown agrégeant tous les sites suivis (synthèse score moyen/max + répartition par classe, répartition par département via `departementName`, tableau détaillé par site trié par score décroissant, correspondance + avertissement). Bouton « 📄 Rapport ESG » sur le dashboard (`SitesDashboard.tsx`, `onExportReport`) → télécharge un `.md` (`portfolioReportFilename`). Assemble `sorted` + `statuses` + `dashboardScore` + `departementCode`. Dégrade proprement (0 site, sites non évalués). Testé (`scripts/test/report.test.ts`, section portefeuille).
- **Export PDF du rapport ESG** (post-Sprint 20, `lib/reportHtml.ts`) : choix retenu = **impression navigateur**, pas de librairie de rendu (cohérent avec le refus des deps lourdes déjà pris pour Playwright — pas d'entrée package.json — et avec le principe local-only, aucun aller-retour serveur). `markdownToHtml(markdown)` convertit le sous-ensemble Markdown **contrôlé** produit par `buildMarkdownReport`/`buildPortfolioMarkdownReport` (titres, tableaux, listes, gras/italique) en HTML sémantique — pas un parseur Markdown générique, seulement ce que les builders émettent réellement. Tout le texte est **échappé** avant insertion (les libellés de site viennent de la BAN, donc du texte utilisateur). `reportPrintHtml(markdown, title)` enveloppe le résultat dans un document HTML autonome avec CSS `@media print` (masque la barre d'action à l'impression, évite les coupures de tableau/liste) et un bouton « 🖨️ Imprimer / Enregistrer en PDF » (`window.print()`). Boutons « 🖨️ Version PDF » (fiche site, `HomeClient.tsx`) et « 🖨️ PDF » (portefeuille, `SitesDashboard.tsx`) à côté du bouton Markdown existant, même flux de données (aucune duplication de la logique d'assemblage). ⚠️ **`window.open()` doit rester dans le préfixe synchrone du handler** (avant tout `await`) sinon les bloqueurs de popup l'interceptent — c'est pourquoi la fenêtre est ouverte avec un HTML de chargement *avant* le fetch de la projection, puis son contenu est réécrit une fois le rapport prêt (`HomeClient.tsx`, `exportReport`). **Repli si popup bloquée** : téléchargement du HTML imprimable en `.html` (le fichier s'ouvre et s'imprime localement). Testé (`scripts/test/report.test.ts` : rendu des titres/tableaux/listes/gras, échappement HTML sur un libellé malveillant, document complet).
- **Projections 2050** (`lib/projections.ts` + `data/projections/`) : données réelles Explore2 TRACC **par commune (bassin versant)**, lookup par code INSEE (arrondissements 751xx/132xx/6938x normalisés vers 75056/13055/69123), repli lat/lon → commune via geo.api.gouv.fr. 96 shards JSON par département, embarqués via `outputFileTracingIncludes` dans `next.config.ts`. `meta.json` porte la provenance et le flag `demo` (bandeau UI automatique).
- **Fenêtre d'historique portée à 14 ans** (Sprint 27, `WINDOW_YEARS`) — **le facteur limitant était la fenêtre, pas la source** : le CSV couvre 2010→2026 mais la couverture observée en prod démarrait au 2017-01-01. Coût **mesuré au banc** (3 runs) : ~1 900 ms à 10 ans contre ~2 600 ms à 14, soit +37 %. ⚠️ **Le banc sous-estime l'absolu d'un facteur ~6** : son fichier synthétique fait 3,4 Mo quand le vrai en fait 11. Mesuré en prod le 2026-08-05, `/api/history` répond en **11,1 s à 10 ans** ; à +37 %, la fenêtre de 14 ans situe donc la route autour de **15 s pour un budget de 60**. Marge de 4×, pas de 40× — le banc sert à comparer des réglages, jamais à juger de l'absolu. 14 atteint 2013 et s'arrête avant 2010-2012 où le fichier s'amincit réellement (24 arrêtés en 2010). Effet mesuré sur le banc : la moyenne structurelle passe de 74 à 69 j/an — la fenêtre de 10 ans reposait sur 2017-2025, qui contient **2022 et 2023**, deux sécheresses exceptionnelles, et **surestimait** donc. ⚠️ **Élargir amplifie un biais** : `joursAlertePlusMoyen` divise par toutes les années couvertes par le FICHIER, donc une zone créée en 2022 se voit attribuer des années à zéro avant son existence. Ni diviser par les années du fichier (sous-estime) ni par les siennes (surestime, c'est le bug corrigé au Sprint 26) n'est juste : le dénominateur prudent est conservé et **l'ambiguïté est exposée** par `ZoneHistory.premiereAnnee` au lieu d'être tranchée en silence.
- **Historique** (`lib/history.ts`) : source primaire = CSV maître « **Arrêtés** » (`f425cfa6…`, ~11 Mo, MAJ quotidienne, couvre **2012→**). Une ligne = un arrêté, zones en **tableaux JSON parallèles** (`zones_alerte.code` / `.id` / `.niveau_gravite`) que le parseur explose ; schéma ligne-par-zone toujours supporté en repli. **Agrégation multi-années sur une fenêtre glissante de 5 ans** (`WINDOW_YEARS`) : chaque zone porte `parAnnee` (détail par année) + `joursAlertePlusMoyen`/`anneesCompletes` (moyenne jours/an en alerte+ sur les années **complètes**, année en cours exclue de la moyenne). Dédup par jour au niveau max, indexé par code zone ET id numérique. ⚠️ dates corrompues (`debut` année < 2005, ex. 0022) **écartées** (sinon bornées à la fenêtre → jours fantômes). ⚠️ **« Arrêtés Cadre » (`0732e970…`) n'a pas de colonne gravité** — jamais utilisable ; ⚠️ `niveau_gravite_specifique_aep` ne doit pas matcher le motif gravité. **`/api/history?zones=x&debug=1`** révèle chaque tentative (+ `windowYears`, `parAnnee`) ; test : `npx tsx scripts/test/history-parser.test.ts`.
- **Onde / assecs** (`lib/onde.ts` + `/api/onde`) : réseau sentinelle OFB via Hub'Eau `/v1/ecoulement/observations` (bbox 60 km, campagne des 45 derniers jours). `classifyEcoulement` mappe libellé/code → assec/nonVisible/faible/visible ; sévérité 100/65/30/0 moyennée → risque 0-100. **Saisonnier (mai-sept)** : hors saison, pas d'obs récente ⇒ `null` ⇒ composante absente (jamais inventée). Test : `npx tsx scripts/test/onde-classify.test.ts`.
- **Références standardisées** (`lib/hubeau.ts`, `computeIps` / `computeLowFlow`) : calculées **en interne** depuis l'historique Hub'Eau (pas d'API ouverte propre pour Hydroportail/BRGM). IPS nappe = rang du niveau du mois courant dans la distribution du même mois calendaire (≥10 ans) ; VCN10/QMNA5 = quantile 0,2 des minima annuels (10 j glissants / mensuels) sur ≥6 ans de QmnJ, débit récent comparé au VCN10. Attachées à `IndicatorResult.reference` → remontent au score (composantes hydro/piezo), repli sur la tendance 14 j si historique trop court. Test : `npx tsx scripts/test/reference-stats.test.ts`.
- ⚠️ **Pièges Hub'Eau vérifiés en réel (Sprint 9)** : (1) le débit journalier élaboré est **`grandeur_hydro_elab=QmnJ`**, PAS `QmJ` (ce dernier → HTTP 400 « Pattern »). (2) le référentiel piézo `/v1/niveaux_nappes/stations` n'a **pas** de `longitude`/`latitude` : coordonnées dans `x`/`y` (WGS84 en `format=json`) et `geometry` (vide en `format=json`, rempli en `format=geojson`) — extraire `geometry.coordinates` avec repli `x`/`y`. Ces deux bugs faisaient échouer silencieusement les cartes débit et nappe (repli hauteur / « aucun piézomètre »).
- **BNPE / prélèvements** (`lib/bnpe.ts` + `/api/bnpe` + `BnpePanel`, Sprint 10-11) : Hub'Eau `/v1/prelevements/chroniques?code_commune_insee=…` → `aggregateBnpe` somme les volumes par usage sur l'année la plus récente (usages normalisés, volumes ≤ 0 ignorés) + contexte commune (surface km² / population via geo.api → intensité m³/km² et m³/hab). **Bloc informatif seulement — hors score composite.** ⚠️ Vérifié : la chronique BNPE **n'a pas de champ milieu** (surface/souterrain), donc pas de ratio prélèvements-surface/débit ; commune ≠ bassin ; pas de ressource par sous-bassin en open data → un score BNPE fiable est bloqué sur ces données, ne pas le forcer. Vérifié réel : Chartres 819 072 m³ (48 400 m³/km²), Toulouse 62 Mm³ (526 000 m³/km²), 2023. Test : `npx tsx scripts/test/bnpe.test.ts`.
- **Carte des ressources** (Sprint 29, `lib/carteEau.ts` + `/api/carte` + `CarteEau.tsx`/`CarteClient.tsx`) : page `/carte`, les objets **physiques** de la ressource autour d'une adresse — stations de débit, piézomètres, observations ONDE, ouvrages BNPE, sur fond de nappes affleurantes. **C'est un repère, pas un modèle** : rien n'entre dans `computeScore` (même règle de non-double-comptage que `secteur`/`origine`). ⚠️ **Ne pas réutiliser `/api/hydro` ni `/api/piezo`** pour ça : elles rattachent **une** station en sondant chaque candidate (8 × 1-2 appels) et `StationOption` **ne porte pas de coordonnées**. Chaque couche est fetchée indépendamment et **échoue seule** ; une couche qui revient avec une **page pleine** (`size` atteint) affiche « vue incomplète » plutôt que d'avoir l'air complète — mesuré : le référentiel piézo renvoie **500/500 à 60 km**, et sa troncature ne suit **pas** la distance. ⚠️ **Deux incomplétudes à ne pas confondre** : troncature **amont** (le serveur s'est arrêté sur *son* ordre, on ignore ce qui manque → avertissement) et **notre** plafond `MAX_FEATURES_PER_LAYER` (on a gardé les 300 plus proches, tout ce qui manque est plus loin → simple constat). Test : `npx tsx scripts/test/carte.test.ts`.
- **⚠️ Les ouvrages BNPE saturent une page à TOUS les rayons** (Sprint 29, mesuré de bout en bout) : le réseau de prélèvement est bien plus dense que les réseaux de mesure — **10 km sur Lyon suffisent à remplir 500 lignes**. D'où `BNPE_MAX_ROWS = 5000` pour cette couche seule (taille déjà utilisée par `lib/bnpe.ts` depuis le Sprint 10 sur le même référentiel). ⚠️ **Le plafond client de 300 reste atteint partout** — la lisibilité de la carte à cette densité **n'a jamais été observée**.
- **Validé en réel le 2026-08-05** (diag mode `carte`, run 32, `/api/carte` construit et exécuté sur le runner) : Chartres 30 km → 10 débit / 24 piézo / 13 ONDE ; Lyon 10 km → 10 / 7 / 2 ; Perpignan 60 km → 83 / 91 / 46. **Le filtre de rayon tient exactement** (distances maximales 29,8 / 9,8 / 59,9 km — aucun point de la bbox carrée ne fuit hors du disque), les libellés sont réels (« L'Eure à Lèves » à 2,6 km, « La Saône à Lyon [Pont La Feuillée] » à 0,9 km), et le classifieur ONDE lit les vrais libellés (**13/13, 2/2, 46/46** observations classées, textes non accentués). ⚠️ **178 ouvrages sur 300 autour de Perpignan sont au centroïde de leur commune** — sans le signalement, la majorité de la couche mentirait sur sa position.
- **⚠️ Les ouvrages BNPE ont des coordonnées — et le référentiel dit lui-même quand elles sont fausses** (Sprint 29, mesuré) — lève l'item 8 bis du backlog (« non instruit »). `referentiel/ouvrages` porte `longitude`/`latitude` **et** un `geometry` CRS84. Mais il porte aussi `code_precision_coord` / `libelle_precision_coord`, et la valeur **`5` = « Coordonnées du centroïde de la commune »** : ces points disent où est la mairie, pas où est la pompe. Ils sont **conservés** (masquer un prélèvement déclaré serait pire) et **signalés** — cercle translucide + mention dans la popup. ⚠️ `libelle_usage_principal` **n'existe pas** sur ce référentiel (l'usage est sur les chroniques, cf. `lib/bnpe.ts`) : le demander met la couche entière en 400.
- **⚠️ Le WFS Sandre filtre QUELLES entités il renvoie, jamais leur résolution** (Sprint 29, mesuré) — `sa:MasseDEauSouterraine_VRAP2022_FXX` : **639 entités, 237 Mo** en national, **108 Ko pour une seule entité**, et **19,5 Mo pour une requête BBOX d'un seul viewport**. Donc ni embarquer le brut ni interroger à la volée : le facteur limitant est la **précision géométrique**. `scripts/refdata/fetch_nappes.py` (mode `nappes` du workflow refdata) télécharge une fois, garde les **621 masses d'eau affleurantes** (`SurfaceAffKm > 0` — dessiner l'emprise d'une masse d'eau profonde par-dessus celle qui affleure dirait que l'eau est là où elle n'est pas), simplifie en **Lambert-93** (tolérance en vrais mètres) et **descend une échelle de tolérances jusqu'à tenir un budget d'octets** : 200 m → 3,78 Mo, **400 m → 2,35 Mo**, retenu. Servi par `/api/nappes` (jumeau de `/api/departements`) — ⚠️ **entrée `outputFileTracingIncludes` obligatoire**, son oubli ne casse que la prod.
- **⚠️ MapLibre : `map.on("load")` n'est PAS le bon signal pour ajouter des couches** (Sprint 29, mesuré en bac à sable) — `load` **et** `map.isStyleLoaded()` attendent que **toutes les sources** soient stabilisées, fond raster compris. Hôte de tuiles injoignable ⇒ l'évènement ne part **jamais**, **aucune couche n'est installée**, et même `/api/nappes` — servi localement, sans réseau — **n'est jamais requêté**. Le bon signal est **`styledata`** (le style *inline* est parsé), avec un drapeau `installed` puisque l'ajout de sources le refait partir. Corrigé sur `CarteEau` **et sur `ZonesMap`**, qui portait le même piège en silence : en prod les tuiles répondent, donc personne ne l'avait vu. ⚠️ Conséquence de méthode : **un rendu vide en bac à sable n'est pas forcément « pas de données »** — le conteneur porte désormais `data-map-ready` pour distinguer « carte construite, rien à dessiner » de « carte jamais initialisée », et l'e2e l'asserte.
- **Groupement des positions administratives** (Sprint 30, `finalize()` dans `lib/carteEau.ts`) : plusieurs objets publiés **exactement à la même position** deviennent **un marqueur portant leur nombre**, dont la popup les liste. Clé de groupement : coordonnées arrondies à **5 décimales (~1 m)** — deux ouvrages réellement relevés ne tombent jamais sur le même mètre, deux centroïdes de la même commune si. ⚠️ **Le groupement précède le plafond** `MAX_FEATURES_PER_LAYER` : plafonner d'abord dépenserait les 300 places en doublons d'une poignée de communes et jetterait des communes entières plus proches. ⚠️ **Le compteur affiché compte des OBJETS, le plafond compte des MARQUEURS** — d'où `MapLayers.totals` (via `countObjects`) à côté de `features` : sans lui « 300 ouvrages » deviendrait silencieusement « 120 marqueurs ». **Pas d'éclatement en pétale** : ces coordonnées sont administratives, les écarter dessinerait des positions que le référentiel ne publie pas. **Mesuré en réel** (diag `carte`, run 34) : Chartres 460 ouvrages → 300 marqueurs dont 131 groupés ; Lyon 559 → 273 (aucun plafond atteint) dont 117 groupés ; **Perpignan 706 → 300 dont 116 groupés, et un seul marqueur y cachait 48 ouvrages**.
- **Popups « nom + caractéristiques »** (Sprint 30, `popupHtml()` / `nappePopupHtml()` dans `CarteEau.tsx`) : chaque objet se présente au clic. ⚠️ **Tout champ affiché a été observé dans une vraie réponse** (diag `carte2`) — Hub'Eau répond **400 sur un `fields=` inconnu**, donc un nom de colonne plausible n'est pas une colonne. Champs confirmés : hydro `libelle_cours_eau`/`libelle_commune`/`type_station`/`date_ouverture_station`/`altitude_ref_alti_station` (**`libelle_site` existe bien** — risque du Sprint 29 refermé) ; piézo `noms_masse_eau_edl`/`profondeur_investigation`/`altitude_station`/`date_debut_mesure`/`nb_mesures_piezo` ; ONDE `libelle_station`/`libelle_cours_eau`/`libelle_bassin`/`libelle_reseau` (**les observations portent bien un libellé** — second risque refermé) ; BNPE `nom_commune`/`date_exploitation_debut`. ⚠️ **Une caractéristique sans valeur est retirée, jamais rendue en « — »** (ce serait une absence mesurée). Les polygones de nappes sont cliquables et **listent toutes les masses d'eau sous le curseur** (elles partagent des bords), sans en élire une.
- **Liens « fiche officielle » : seulement là où le référentiel publie une URL** (Sprint 30) — jamais fabriquée à partir d'un motif d'URL. ⚠️ Le nom de colonne ment dans les deux sens : **`urn_bss` (piézomètres) contient bien une URL http** vers ADES, tandis que l'hydrométrie ne publie **que `uri_cours_eau`, aucune URI de station** — ces marqueurs n'ont donc **pas** de lien. `httpUrl()` ne retient une valeur que si elle commence par `http(s)://`. ONDE : `uri_station` ; BNPE : `uri_ouvrage`.
- **Cours d'eau** (Sprint 30, `sa:MasseDEauRiviere_VRAP2022_FXX` + `scripts/refdata/fetch_cours_eau.py` + `/api/cours-eau`) : **le pendant surface exact de la couche de nappes déjà embarquée** (même référentiel, même version de rapportage). **9 746 masses d'eau cours d'eau** en métropole, LineString, `NomMasseDEau` + **`StrahlMax` (ordre de Strahler)** pour la hiérarchie. Écartés : le réseau Topage complet (centaines de milliers de tronçons) et les couches `CoursEau_classe1..7_Carthage` (référentiel retiré). ⚠️ **La simplification ne sert presque à rien ici** : 7,64 Mo à 150 m contre 5,64 Mo à 1 200 m (‑26 % pour 8× de tolérance) — les coordonnées sont déjà arrondies à ~100 m, donc le coût est le **nombre d'entités**, pas la précision. D'où un choix de conception différent des nappes : **fichier complet sur disque (5,84 Mo, tolérance 600 m), filtrage à la requête**. `/api/cours-eau?bbox=` renvoie les cours d'eau de l'emprise (**~50 Ko**, mesuré sur Chartres et Perpignan) ; sans bbox, seuls les **Strahler ≥ 5** (569 rivières, 716 Ko) pour la vue France. ⚠️ Un premier essai avec un budget de 2 Mo pensé pour un téléchargement intégral avait retenu **Strahler ≥ 5 seulement — 6 % du réseau**, laissant la plupart des adresses sans rivière.
- **⚠️ `Karstique` et `MultiCouches` ne sont PAS renseignés** dans `sa:MasseDEauSouterraine_VRAP2022_FXX` (Sprint 30, mesuré sur 200 masses d'eau : **0 partout**, y compris « Calcaires du Boulonnais », « Calcaire carbonifère de Roubaix-Tourcoing », « Jura alsacien »). Ne pas les afficher — « Karstique : non » sur les Causses serait un fait inventé. En revanche `NatureEcoulement` (1/2/3/4) et `TypeMasseDEauSouterraine` (A/DS/IL/IP/S) **varient** : exploitables le jour où l'on récupère la nomenclature Sandre correspondante, non affichés faute de la connaître.
- **⚠️ `LongueurTotKm` des masses d'eau rivière est inexploitable en l'état** (Sprint 30) : médiane 38 (plausible en km) contre un maximum de **180 748** (plausible en mètres, impossible en km). Unité incohérente à l'intérieur d'une même colonne ⇒ **non affichée**, la propriété reste dans le fichier pour un sprint qui l'établira.
- **Registre de couches unique et groupé** (Sprint 31, `LAYERS` / `LAYER_GROUPS` dans `lib/carteEau.ts`) : **une seule liste** décrit toutes les couches — points, lignes ET surfaces — avec son `groupe`, sa `forme`, sa couleur et sa **`description`**. Avant, les nappes et les cours d'eau étaient deux booléens ad hoc traversant les composants pendant que les points vivaient dans une liste ; ajouter deux couches par ce chemin en aurait fait quatre. Les trois groupes sont les trois questions du lecteur : **« Où est l'eau » · « Qui la mesure » · « Qui la prélève »** — une station n'est pas une source, un forage non plus : l'une mesure, l'autre prend.
- **⚠️ Les descriptions s'affichent DANS la page, pas en `title`** (Sprint 31) : l'infobulle `title` **n'existe pas sur écran tactile**, et c'est précisément là que se pose la question « c'est quoi un piézomètre ? ». Panneau « Comprendre la carte » sous la carte.
- **⚠️ Pas d'encart en surimpression sur la carte, et UNE SEULE popup** (Sprint 31, signalé depuis un vrai téléphone) : la légende flottante couvrait un tiers de l'écran mobile et **entrait en collision avec chaque popup**, tout en dupliquant la barre de bascules (mêmes pastilles, mêmes libellés, plus les compteurs). Supprimée. ⚠️ MapLibre ouvre **volontiers plusieurs popups à la fois** : une instance partagée (`popupRef`) rend « un objet décrit à la fois » structurel, et le marqueur d'adresse a perdu la sienne (l'adresse est déjà écrite dans le champ au-dessus). Un test e2e interdit désormais tout encart flottant autre que le bouton de recherche.
- **Captages d'eau potable = jointure, pas source nouvelle** (Sprint 31, `parseUsageByOuvrage`) : la BNPE publie l'usage sur ses **chroniques**, pas sur le référentiel des ouvrages ; la jointure par `code_ouvrage` donne donc l'usage de chaque ouvrage, et les captages AEP sont ceux d'usage « EAU POTABLE ». **Couverture mesurée : 1 498/1 820 (82 %) à Chartres, 607/607 et 3 143/3 143 (100 %) à Lyon et Perpignan.** **Validé de bout en bout** (diag `carte`, run 37) : **115 captages** rendus autour de Chartres, 2 à Lyon, **215 à Perpignan** (ex. « FORAGE EN NAPPE MAS BRUNO », EAU POTABLE, souterrain, à 2,1 km). ⚠️ **Les chroniques comptent une ligne PAR AN ET PAR OUVRAGE** — 16 566 lignes à Chartres pour 1 820 ouvrages : demander la taille de page du référentiel (5 000) aurait perdu l'usage de la plupart des ouvrages, d'où `BNPE_USAGE_MAX_ROWS = 20000`. ⚠️ **Un ouvrage non atteint par la jointure a un usage INCONNU, pas « autre »** : il affiche « usage non renseigné », reste dans les autres prélèvements, et un test l'exige. Sans chroniques du tout, **aucun** captage n'est déclaré. `normalizeUsage` (`lib/bnpe.ts`) est réutilisé — mais **seulement sur un libellé existant**, puisqu'il répond « Autres » sur une chaîne vide.
- **Plans d'eau** (Sprint 31, `sa:PlanEau_FXX_Topage2026` + `fetch_plans_eau.py` + `/api/plans-eau`) : **34 513 entités, 205 Mo** en national, `TopoOH` pour le nom (**vide 4 fois sur 10** : 19 540 nommés) et `NaturePE` pour la nature (« retenue », « gravière »…). ⚠️ **Le référentiel ne publie AUCUNE surface** : elle est **calculée** en Lambert-93 par le script et sert de premier filtre. Médiane nationale **1,9 ha** ; retenu **≥ 5 ha, tolérance 20 m → 7 563 entités, 5,57 Mo**. Servi comme les cours d'eau : fichier complet sur disque, **filtre bbox à la requête** (~52 Ko autour de Chartres) ; sans bbox, les ≥ 100 ha seulement (410 entités). Le seuil de 5 ha est **écrit dans l'interface**, pas tacite.
- **État d'un objet de la carte** (Sprint 32, `stationEtat` dans `lib/hubeau.ts` + `/api/carte/etat` + `etatHtml` dans `CarteEau.tsx`) : au clic, la popup montre **les mêmes chiffres que la fiche site** — dernière mesure, date, tendance 14 j **avec son libellé**, référence standardisée (IPS piézo / VCN10 débit) avec sa base et ses années de recul, et une sparkline. **Un appel par clic**, jamais en amont : sonder la chronique de chaque station visible coûterait des centaines d'appels pour servir une popup. ⚠️ **Ne PAS passer par `hydroIndicators(lat, lon, code)`** : elle télécharge d'abord tout le référentiel de la bbox pour *choisir* une station, travail perdu quand le code est déjà connu.
- **⚠️ Trois natures d'« état », trois vocabulaires** (Sprint 32) : une station **mesure** la ressource (niveau, tendance, référence) ; un ouvrage **exerce une pression** sur elle (dernier volume déclaré, **avec son année** — la déclaration a 2-3 ans de retard, la plus récente publiée était 2023) ; une masse d'eau **n'a pas d'état physique national** — la popup montre donc le niveau d'arrêté **réglementaire** de la zone, et le dit (l'état quantitatif des masses d'eau est instruit et clos depuis le Sprint 27, cf. §5).
- **⚠️ « Service injoignable » n'est PAS « station muette »** (Sprint 32) : `stationEtat` renvoie `"service-indisponible"` quand la sonde rend `null` (Hub'Eau n'a pas répondu) et `"station-muette"` quand elle rend `available: false` (la station ne publie rien d'exploitable). Confondre les deux accuserait une station en bonne santé pendant une panne. Trouvé en bac à sable : la première version disait « cette station ne publie pas de mesure récente » alors que l'egress était simplement bloqué.
- **⚠️ La référence ne prend pas la popup en otage** (Sprint 32, `ETAT_REFERENCE_BUDGET_MS = 6000`) : elle télécharge 18 à 25 ans d'historique (`REF_TIMEOUT_MS` vaut déjà 15 s). Elle est lancée **en parallèle** de la mesure et **abandonnée** au-delà du budget ; la popup affiche alors la mesure et **dit** que la référence manque. Une popup vide 15 s serait pire qu'une popup partielle. **Budget corroboré par la mesure** (diag `carte`, run 38) : la référence la plus lente est arrivée en **3,4 s** sur six stations réelles (IPS sur 26 ans à Chartres, 21 ans à Lyon ; VCN10 sur 19 ans à Perpignan), soit 1,8× de marge — et un ouvrage répond en 0,15 s, une zone réglementaire en 0,83 s. ⚠️ Le chemin « référence abandonnée » n'a donc **jamais été exécuté en réel**.
- **⚠️ Une popup plus riche est une popup plus haute** (Sprint 32) : l'ajout de l'état a triplé sa hauteur, et sur téléphone elle débordait la carte **en repassant sous le bouton flottant** — le défaut du Sprint 31 revenu par une autre porte. Bornée à **240 px avec défilement interne** (mesuré : 338 px sans borne débordait de 90 px, 270 px n'en déborde plus que de 22), et le bouton « Rechercher dans cette zone » **s'efface** tant qu'une popup est ouverte.
- **Géométrie de sparkline partagée** (`lib/sparkline.ts`) : `components/Sparkline.tsx` (React) et les popups de la carte (chaînes HTML) tracent la **même** courbe. ⚠️ Une série plate est dessinée **à mi-hauteur**, pas en haut : un `span` nul placerait la ligne au sommet et se lirait « niveau haut ».
- **Accueil = landing** (`components/Landing.tsx`) : au repos (`!loading && !data`), `HomeClient` affiche la landing marketing ; la grille résultats + carte ne s'affiche que pendant/après une recherche (`loading || (address && data)`).
- **Tendance 14 j** (`lib/hubeau.ts`) : moyenne 7 derniers jours vs 7 précédents, **rapportée à l'amplitude de la fenêtre** (pas à la moyenne — sinon un niveau NGF ~100 m serait toujours « stable »). Sens inversé pour les profondeurs de nappe.
- **Choix utilisateur persistés** : sites `hydrovigie.sites.v1`, stations `hydrovigie.stations.v1` (localStorage). Deep links `/?lat&lon&label&profil&ccode` relancent l'analyse complète.

## 2 bis. Heuristique — trois fois de suite, le blocage était dans la formulation

Trois sprints consécutifs ont levé un verrou que ce HANDBOOK déclarait bloqué, **sans qu'aucune
source nouvelle n'apparaisse**. À chaque fois, la donnée manquante n'était pas absente : la question
était mal posée.

| Sprint | Ce que le HANDBOOK disait | Ce qui l'a levé |
|---|---|---|
| 25 | « quelle entité BDLISA choisir pour un point ? » | **Cesser de choisir** — tester l'appartenance ensembliste |
| 26 | « pondérer par les volumes : VigiEau n'en publie aucun » | **Changer de détenteur** — l'entreprise connaît les siens |
| 27 | « ressource renouvelable par sous-bassin : pas de jeu de données » | **Calculer au lieu de chercher** — le module était dans une série déjà téléchargée |

**Réflexe à appliquer avant d'accepter un « bloqué » écrit ici** : la donnée est-elle vraiment
absente, ou seulement absente *sous la forme où je la cherche* ? Trois questions qui ont marché —
puis-je la **produire** avec ce que j'ai déjà ? quelqu'un d'autre la **détient-il** (l'utilisateur,
typiquement) ? la question exige-t-elle vraiment de **trancher**, ou une réponse ensembliste
suffit-elle ? ⚠️ Cela ne veut pas dire qu'aucun blocage n'est réel : la ZRE hors métropole, la
prévision MétéEAU, QMNA5 dans Explore2 et l'état quantitatif des masses d'eau ont tous été instruits
sérieusement et sont **réellement** clos.

## 3. Environnement de dev (bac à sable Claude) — pièges vécus

- ⚠️ **Le conteneur peut être réinitialisé ENTRE DEUX TOURS d'une même session.** Vécu 2026-08-06 :
  `node_modules` supprimé **et arbre de travail revenu au commit d'avant la session**. Rien n'a été
  perdu parce que chaque sprint avait été poussé au fur et à mesure. **Leçon : pousser à chaque
  sprint, jamais en fin de session.** Reprise : `git fetch origin <branche>` →
  `git reset --hard origin/<branche>` → `npm ci` → `npm i --no-save playwright` → `npm run build`.
  Le symptôme trompe : `next: not found` puis « Could not find a production build » ressemblent à une
  panne d'outillage, pas à une perte de contexte.
- ⚠️ **Pour VOIR une page qui dépend de l'egress : bouchonner les routes en Playwright.** Vécu aux
  sprints 34-36 — la fiche site ne se peuple jamais en bac à sable, donc `page.route("**/api/**", …)`
  avec des charges utiles de forme conforme est le **seul** moyen de regarder la mise en page. ⚠️
  **Deux de mes bouchons se sont trompés de forme** (`TransitionPayload` est **plat**, pas imbriqué ;
  `IndicatorResult` exige `latest`, `grandeur`, `confidence`) : lire le type avant d'écrire le
  bouchon, et se souvenir qu'un plantage sous bouchon est **d'abord** une erreur de bouchon.
  ⚠️ Les captures obtenues ainsi valident **la mise en page, pas les chiffres** — les valeurs sont
  inventées, et il faut l'écrire dans le compte rendu.
- **Egress bloqué** vers TOUS les hôtes français open-data + vercel.app (403 CONNECT du proxy — politique, ne pas réessayer). npm/pypi accessibles. WebFetch pareil.
  → **Contournement établi : GitHub Actions comme exécuteur distant.** Modifier `data/extract-request.json` (mode `discover` | `extract`) et pousser → `.github/workflows/extract-projections.yml` s'exécute avec réseau complet et **committe ses résultats sur la branche**. Attendre via un Monitor qui fait `git fetch` en boucle. Pattern réutilisable pour toute donnée inaccessible.
  → **Variante Sprint 21 : `data/restrictions-probe-request.json` → `.github/workflows/probe-restrictions.yml`**, avec deux modes (`probe` = caractériser les sources et écrire `data/restrictions/probe.json` ; `build` = produire `guide.json` + `zones/*.json`). ⚠️ Le workflow n'écoute **que la branche de la session** — penser à mettre à jour `on.push.branches` en changeant de branche.
  → **Variante diagnostic : `data/diag-request.json` → `.github/workflows/prod-diag.yml`** (résultats dans `data/diag/`, à purger après analyse). Mode `prod` = sonde le déploiement + les sources upstream ; mode `app` = **build et démarre l'app sur le runner** puis sonde localhost (`/api/history?debug=1`, `/api/pmtiles` en Range, zones, projections, hydro) — c'est l'équivalent d'un staging avec réseau réel, utilisé pour valider le correctif historique et la route PMTiles sans déploiement.
  → ⚠️ **`data/diag-request.json` redéclenche le workflow à CHAQUE modification** — y compris quand on remet simplement le mode à `prod` en fin d'analyse. Le runner committe alors sa sortie sur la branche, et **le prochain `git push` est rejeté** (non fast-forward). Vécu 2026-08-05. Réflexe : `git pull --rebase` avant de repousser, et se souvenir que la sortie récupérée est une vraie sonde de prod — à lire avant de la purger, pas à jeter.
  → Tester les intégrations avec les mocks : `scripts/test/hubeau-mock.mjs` + overrides d'env `HUBEAU_BASE_URL`, `VIGIEAU_BASE_URL`, `HISTORY_CSV_URL`.
- **`pkill -f "next start"` se tue lui-même** (le motif matche la ligne de commande du shell) → exit 144. Utiliser `pkill -f "n[e]xt start"` (astuce crochets). Lancer les serveurs de test via tâches en arrière-plan.
  → ⚠️ **`pkill -f "n[e]xt start"` ne suffit pas non plus** (vécu 2026-08-04) : `next start` **délègue à un processus enfant** dont la ligne de commande ne contient pas le motif. Le port reste pris (`EADDRINUSE`), un `next start` relancé échoue en silence, et l'ancien serveur continue de servir les **chunks invalidés par le rebuild** — d'où des e2e qui échouent sur des sélecteurs pourtant corrects. Tuer **par le port** : `fuser -k 3300/tcp`.
  → ⚠️ **Recette qui a fini par marcher (session 2026-08-06, une dizaine d'occurrences)** :
  `pgrep -f next-server | xargs -r kill -9`, **attendre 5 s** que le port se libère — sinon le
  nouveau serveur échoue en `EADDRINUSE` **en silence** et l'ancien continue de répondre, ce qui
  donne exactement le symptôme du `ChunkLoadError` —, parfois `rm -rf .next`, puis lancer avec
  **`setsid nohup npx next start -p PORT … & disown`** : sans `setsid`/`disown`, le serveur est tué
  à la fin de l'appel shell qui l'a démarré. ⚠️ **`pkill -f "next"` est refusé par le harnais** (le
  motif attrape le processus CLI de Claude).
  → ⚠️ **Le symptôme trompe** : un serveur qui sert d'anciens chunks fait échouer l'e2e sur des
  sélecteurs **sans rapport** avec ce qu'on vient de modifier (« nav badge shows 2 »). Réflexe :
  avant de chercher une régression d'interface, vérifier qu'un chunk référencé par le HTML existe
  bien sur disque.
  → ⚠️ **Ne jamais découper un fichier par `s.index(a):s.index(b)` sans vérifier que `a` précède `b`.** Vécu 2026-08-05 sur ce fichier : les deux ancres étaient dans l'ordre inverse, la tranche valait `""`, et `str.replace("", new)` a inséré le texte **entre chaque caractère** — HANDBOOK à **175 Mo**, push rejeté par la limite GitHub de 100 Mo. Le signal était pourtant sorti (`grep -c` à 516 727 lignes au lieu de 220) et n'a pas été lu. **Réflexe : éditer par numéro de ligne ou via l'outil `Edit`, et asserter le nombre de lignes avant/après.** Réparation : `git checkout <dernier bon commit> -- <fichier>` puis `git commit --amend`, le blob n'ayant jamais été poussé.
- **Rebuild pendant qu'un `next start` tourne** invalide les chunks servis → pages cassées, tests qui échouent mystérieusement. Toujours redémarrer le serveur après un build.
- **create-next-app** : refuse les majuscules dans le nom → scaffolder dans un répertoire temporaire ; **ne pas `cp -a` le `.git` du scaffold** (a écrasé le dépôt en Sprint 1 — réparé, mais historique fusionné).
- **Next 16** : lire `node_modules/next/dist/docs/` avant d'écrire (cf. AGENTS.md). ESLint bloque `setState` synchrone dans un effet et la lecture de refs pendant le rendu → patterns utilisés : init paresseuse `useState(() => …)`, état de chargement dérivé d'un mismatch de clé, `setTimeout(…, 0)` pour un fetch initial. `cookies()` est async. Pas de `next/font/google` (réseau bloqué au build local).
- **Playwright** préinstallé (`/opt/pw-browsers`) ; suite de non-régression : `scripts/test/e2e.mjs` (**62 checks** au 2026-08-06), `BASE=http://localhost:PORT node scripts/test/e2e.mjs` (installer playwright hors package.json : `npm i --no-save playwright`). Ne pas l'ajouter aux deps du projet (alourdirait le build Vercel). ⚠️ **Ajouté par erreur en devDependency au Sprint 33, retiré au Sprint 37** — Vercel installe les devDependencies au build, la convention tient.
- Vercel : l'erreur « No Output Directory named public » = preset framework mal détecté → réglé par `vercel.json` (`"framework": "nextjs"`) — ne pas le supprimer.

## 4. Bugs connus / dette

- **Déploiement Vercel** : réglé. `main` mergé (PR #2), l'alias `water-risk-saa-s.vercel.app` sert de nouveau l'app et les correctifs Sprint 7 sont vérifiés en réel dessus. L'« historique cassé en prod » d'avant venait du couple {déploiement absent + bugs source/schéma} désormais corrigé.
- **Comptes/alertes/API retirés (Sprint 8)** : le code Supabase/Resend/API-v1 (jamais testé en réel) a été supprimé — l'utilisateur ne veut pas de login. Récupérable dans l'historique git (commits Sprint 6 sur la branche) si besoin un jour, mais à ne pas remettre sous forme de login.
- Rattachement des stations de **débit** par distance (pas par sous-bassin) — limite documentée. Les **piézomètres** sont désormais rattachés par aquifère BDLISA (Sprint 25).
- Vieille interrogation non tranchée : périmètre ZAS Sandre vs périmètre VigiEau appliqué (cf. PLAN.md §Limites).
- L'historique multi-années est désormais à portée de main : le CSV maître « Arrêtés » couvre 2012→aujourd'hui ; il suffit d'élargir la fenêtre d'agrégation (année en cours actuellement) — prévu Sprint 9.

## 5. Prochaines étapes (par valeur décroissante)

> **⚠️ Priorité absolue au 2026-08-06 : regarder la preview Vercel.** Les sprints 33→37 ont livré une
> refonte complète de l'interface **sans qu'aucun n'ait été confronté à une réponse VigiEau réelle**
> (egress bloqué, tout vu à travers des bouchons Playwright). Cinq sprints s'empilent sur du
> non-constaté. Ce qui reste à vérifier de visu, dans l'ordre : les cinq chapitres de la fiche site
> peuplés · le comportement du sommaire collant pendant qu'un bloc s'insère au-dessus (jamais observé)
> · le chargement sous les **16,0 s réelles** de `/api/hydro`, alors que les délais simulés étaient de
> 5 s. **Ne pas ouvrir de sixième sprint avant.**

> **Deux chantiers d'outillage identifiés par la session UI/UX**, dans l'ordre :
> (1) ajouter **`axe-core`** à `scripts/test/e2e.mjs` — **c'est désormais la dette technique la plus
> rentable du projet**, et le protocole lecteur d'écran l'a démontré : quatre défauts trouvés en
> lisant dix arbres à la main, dont un qui rendait muet un correctif livré. Un balayage automatisé
> couvrirait toutes les pages et attraperait les classes de défauts auxquelles personne n'a pensé ;
> (2) **mesurer les contrastes sur fond coloré** (badges de gravité, encarts ambre, classes WRI),
> explicitement laissés de côté au sprint 33 — ⚠️ la palette de gravité est partagée avec MapLibre
> (`lib/gravite.ts`), donc la toucher déplace aussi les couleurs de la carte.

> **À faire faire par un humain** : le test des 10 écrans de
> [`CHECK-LECTEUR-ECRAN.md`](./CHECK-LECTEUR-ECRAN.md), sur iPhone/VoiceOver **et** Android/TalkBack.
> Deux points ne peuvent pas être tranchés autrement : le comportement de `aria-activedescendant` sur
> le combobox d'adresse (**le contrôle sans lequel rien ne se passe**), et le délai d'annulation de
> suppression, fixé à 8 s pour un usage à la souris et **probablement trop court** à l'oreille.

> **Dette reportée trois fois, à planifier plutôt qu'à « prendre au passage »** : `Panel.eyebrow` rend
> un `<p>` et non un titre, donc six panneaux restent absents du plan du document.

> **Note — notifications email (parkée sur décision utilisateur, 2026-07-21).** L'utilisateur a demandé de mettre de côté les notifications email pour l'instant. Rappel du blocage de fond : elles exigent une infrastructure serveur (stockage des abonnements + service d'envoi + cron) qui contredit la décision structurante « local-only, pas de serveur ». **Ne PAS développer sans arbitrage explicite.** Si un jour repris : option newsletter sans login (email + site + jeton de désabonnement), backend minimal à cadrer (hors périmètre Vercel-static actuel — probablement une fonction serverless + un stockage type KV).

> **⚠️ Réécrit le 2026-08-05.** La version précédente portait « bloqué » sur trois chantiers que les
> Sprints 25-27 ont livrés (aquifère BDLISA, pondération par les volumes, ressource renouvelable).
> Un HANDBOOK qui déclare bloqué ce qui est fait est pire qu'un HANDBOOK muet : il décourage de
> rouvrir. Cadrage produit complet et sourcé dans [`IDEATION-PORTEFEUILLE.md`](./IDEATION-PORTEFEUILLE.md).

### Livré depuis, ne plus chercher ici

| Ancien item | Statut |
|---|---|
| Valider le Sprint 26 sur données réelles | ✅ fait (run 29 ; a trouvé le bug de dénominateur) |
| Rattachement station ↔ aquifère (BDLISA) | ✅ Sprint 25 |
| Pondérer l'exposition par les volumes | ✅ Sprint 26 — volumes déclarés par l'entreprise |
| BNPE dans le score / ressource renouvelable par sous-bassin | ✅ Sprint 27 — **calculée**, pas trouvée (hors score par choix, pas par blocage) |
| Rattachement SDAGE / agence de l'eau par commune | ✅ Sprint 24 |
| Coordonnées des ouvrages BNPE (ancien item 8 bis) | ✅ Sprint 29 — **elles existent**, avec un indicateur de précision qui avoue le centroïde de commune |
| Voir une page à l'écran depuis le bac à sable | ✅ Sprint 29 — Playwright + `page.route()` ; méthode réutilisée aux sprints 30-32, elle a trouvé quatre défauts d'affichage |
| « Quelles sources d'eau autour de mon site ? » | ✅ Sprints 29→32 — page `/carte`, huit couches groupées par question, état de l'objet au clic |

### À faire, par valeur décroissante

1. **Import CSV/Excel + géocodage batch BAN** (`data.geopf.fr/geocodage/search/csv/`, POST) — **blocage n°1 du produit.** Les sites s'ajoutent un par un : une entreprise de 80 sites ne peut pas utiliser l'outil, et la corrélation du Sprint 26 ne se calcule que sur les parcs saisis à la main. Prévoir un rapport de géocodage par ligne — un géocodage silencieusement faux est pire qu'un géocodage manquant.
2. **Voir les panneaux Sprint 26-28 à l'écran avec de vraies données.** Les sondes valident les nombres, **jamais l'affichage** : ni le bloc de corrélation ni `RessourcePanel` n'ont été vus autrement qu'en état dégradé. ⚠️ **Signalé au 26, au 27 et au 28** — trois sprints de suite. Soit un déploiement de preview devient systématique, soit il faut acter que ce point ne se règle pas depuis le bac à sable.
   → **Sprint 29 : une méthode qui marche depuis le bac à sable a enfin été trouvée**, et elle a immédiatement trouvé un bug. Playwright + `page.route()` **intercepte les appels `/api/*`** et sert une charge utile réaliste : le rendu est alors réel (React, MapLibre, WebGL), seules les données sont simulées. C'est ainsi qu'ont été vus le cadrage qui ignorait le rayon et la collision de couleurs ONDE — deux défauts qu'aucune sonde de nombres ne pouvait montrer. **À appliquer aux panneaux 26-28** : le blocage n'était pas l'egress, c'était de n'avoir jamais regardé la page.
3. **Fiabilité du service d'eau potable (SISPEA)** — les sites en origine `aep` dépendent de *leur service d'eau*, pas de la nappe : rendement de réseau, interruptions non programmées. Source ouverte jamais exploitée, et probablement la majorité d'un parc tertiaire. Retenu à l'arbitrage du Sprint 27, non instruit.
4. **Dénominateur d'étiage pour la pression** (Sprint 28 l'a rendu plus net) — `pressionCoursEau` divise par le **module**, qui lisse précisément le moment où la contrainte mord. `computeLowFlow` calcule déjà le QMNA5 mais ne l'expose que dans une chaîne de texte. Une pression d'étiage à côté de la pression annuelle serait le chiffre le plus proche de la question « puis-je pomper en août ? ».
5. **Consommer `ZoneHistory.premiereAnnee`** — le champ existe et est testé, **aucun consommateur ne s'en sert**. Il est à moitié livré : afficher « historique partiel, moyenne probablement sous-estimée » là où la zone est récente.
6. **Regarder `/carte` avec un vrai fond de tuiles.** Le run 40 a validé **toutes les données** en prod (§1, entrée du 2026-08-06) mais aucune sonde HTTP ne juge un rendu : le fond raster est injoignable en bac à sable, donc la carte n'a **jamais** été vue avec ses tuiles. Il suffit d'ouvrir `https://water-risk-saa-s.vercel.app/carte` — c'est la seule étape de ce sprint qui demande un œil humain.
7. **Faire tomber le repli « référence abandonnée » au moins une fois** (Sprint 32) — le budget de 6 s tient au runner (3,4 s au pire) **et en prod** (3,1 s), donc ce chemin du code n'a jamais été exécuté ailleurs que dans les tests. Ce n'est plus un risque de production, c'est une **branche non éprouvée** : la faire tomber volontairement (budget abaissé le temps d'un diag) coûte peu et dirait si la popup dégrade proprement.
8. **Flux `/api/feed?zones=…` (RSS/Atom/ICS)** — la sortie du blocage « alertes vs local-only » parké depuis le Sprint 8 : tout l'état tient dans l'URL, à coller dans Outlook/Teams. Zéro compte, zéro stockage serveur, même principe que le lien de partage du Sprint 14.
9. **Sites fournisseurs critiques** (tag « amont ») — l'interruption d'un fournisseur eau-intensif arrête l'usine aval. Probablement la plus grosse valeur latente, parce que personne ne cartographie ce risque.
10 bis. **Identifier le vrai cours d'eau d'une commune.** La réserve combinée du Sprint 28 **signale** le cas Toulouse (75 % « Très élevé » sur l'Hers, où la ville ne puise pas) mais ne le corrige pas : le chiffre reste affiché avec sa classe. ⚠️ **Mis à jour au Sprint 29 : la donnée manquante existe.** Les ouvrages BNPE portent bien `longitude`/`latitude`, **et** `code_entite_hydro_cours_eau` — le rattachement direct au réseau hydrographique, dans le même enregistrement. Deux réserves avant d'y investir : le champ était **`null` sur l'échantillon de Chartres** (couverture à mesurer sur un lot plus large), et `code_precision_coord = 5` signale les ouvrages positionnés au centroïde de leur commune, inutilisables pour ce croisement. **Sonder la couverture de ces deux champs d'abord** ; s'ils tiennent, l'artefact se corrige au lieu de s'annoter.
10. **Bassin versant réel au lieu de la commune** dans `lib/ressource.ts` — via `sa:BVSpeMasseDEauSurface_VEDL2019_FXX` (cf. §2). Lèverait la principale approximation du modèle.
11. **Restrictions non préfectorales** (plafonds ICPE, volumes prélevables et quotas OUGC en ZRE) — vraies causes d'interruption ignorées par l'outil. **Existence en open data non vérifiée : sonder avant de coder.**
12. **Étendre les horizons portefeuille** *fin de saison* et *2050* — aujourd'hui année type seule ; les deux autres exigeraient des appels par site que le dashboard évite délibérément.
13. **Granularité ESRS E3 plus fine** dans le rapport ESG — l'export PDF est fait, reste la profondeur du contenu.
14. **Rafraîchir les données ZRE** périodiquement (le zonage évolue) : relancer `fetch-refdata.yml` en poussant un changement sur `data/refdata-request.json`.

### Réellement bloqués — instruits, motif écrit, ne pas re-prober

ZRE hors métropole · prévision MétéEAU (OAuth2) · QMNA5 et recharge dans Explore2 · **état quantitatif national des masses d'eau** (Sprint 27) · Propluvia `zones_communes.csv` (sans objet) · tarification progressive locale · benchmark du score composite courant (exigerait de scorer toute la France en live).

### Issues connues à surveiller

- ✅ **Trois violations vérifiées de « service injoignable ≠ station muette », trouvées le 2026-08-07**
  par l'audit `silent-failure-hunter`, **recoupées ligne à ligne** puis **corrigées le même jour**
  (voir l'entrée de session ci-dessus et le compte rendu daté). La règle avait été rendue structurelle
  sur la carte au Sprint 32 ; elle n'avait jamais été appliquée en amont. ⚠️ **Corrigées mais non
  vérifiées en réel** : egress bloqué, aucune n'a été vue sur le déploiement. Historique conservé
  ci-dessous parce qu'il dit où le code est fragile — toutes trois avaient la forme du bug du SWI :
  un échec total rapporté comme « rien à signaler ».
  1. **`lib/hubeau.ts:664-679` (hydro) et `:808-813` (piézo)** — `probeHydroFlow` / `probeHydroHeight`
     / `probePiezo` renvoient `null` **uniquement** quand `hubeauJson` a échoué (réseau ou HTTP), ce
     qui est distinct d'un `ProbeOutcome{available:false}` signifiant « la station n'a rien de
     récent ». Or `if (p) probes.set(...)` **jette le `null`**, et `toOption` (`:236-247`) rend
     `available: probe?.available ?? false` : une station dont l'appel série a expiré devient
     indiscernable d'une station réellement muette. ⚠️ **Scénario concret** : l'appel `referentiel/
     stations` sur bbox passe (donc pas de `SERVICE_ERROR`) mais tous les appels `obs_elab` par
     station échouent → l'utilisateur lit « Stations proches sans données récentes de débit ni de
     hauteur. » alors que le service est en panne. C'est exactement le cas du run 35 (2026-08-05), à
     ceci près que là le référentiel avait échoué et le message était juste — **la panne partielle,
     elle, ment**. Se propage à `components/SiteIndicators.tsx:149-171`, qui ne lit que
     `data.selected` et jette `data.message`, puis à `lib/score.ts:87-104` où le composant sort de la
     moyenne pondérée et **baisse la confiance sans jamais nommer la panne**.
  2. **`lib/bnpe.ts:210-234`** — quatre causes (HTTP ≠ 200/206, payload malformé, exception réseau,
     commune sans aucun prélèvement déclaré) retournent le **même `null`**, et `app/api/bnpe/route.ts`
     l'assume dans la copie affichée : « Aucun prélèvement déclaré pour cette commune (**ou service
     indisponible**) ». ⚠️ **La même source est traitée correctement 30 lignes plus loin** dans
     `app/api/carte/etat/route.ts:61-70`, qui sépare « Chroniques BNPE indisponibles » de « Aucun
     volume déclaré » avec le contrat `null` vs `[]` de `hubeauJson`. C'est donc une régression
     locale, pas une limite du client.
  3. **`lib/onde.ts:109-152`** — `rows === null` (service injoignable) et `perStation.size === 0`
     (aucune observation fraîche) renvoient le même `null`, et `app/api/onde/route.ts:14-19`
     **attribue systématiquement la cause à la saisonnalité** : « réseau saisonnier, mai–septembre ».
     Une panne Hub'Eau **en pleine campagne** est donc présentée à l'utilisateur comme un fait normal.
     Alimente le composant de poids 10 de `lib/score.ts:141-148`.
- ✅ **`lib/swi.ts` — la détection d'en-tête pouvait rejouer le bug du SWI si Météo-France renommait
  une colonne** (trouvé et **corrigé** le 2026-08-07). Trois chemins menaient au même « Aucune mesure
  récente pour cette maille » : (a) aucune ligne ne contient `NUMERO` → positions par défaut
  `(0, 3, 4)` appliquées à **toutes** les lignes ; (b) en-tête trouvé mais aucune colonne ne commence
  par `SWI` → `swiIdx = -1`, `Number(cols[-1])` = `NaN`, **toutes** les lignes écartées ; (c) pas de
  colonne `DATE` → `dateIdx = -1`, `period = ""`, et `"" <= ""` fait `continue` sur **chaque** ligne.
  `latestForCell` renvoie désormais `"format-inconnu"`, distinct de `null` (maille réellement vide),
  avec 4 tests de non-régression.
- ⚠️ **Trois `sys.exit(1)` neufs dans les scripts de build (2026-08-07) n'ont JAMAIS été exécutés** —
  ni en succès, ni en échec : ces scripts ne tournent que sur un runner GitHub. Les planchers sont
  chiffrés depuis ce HANDBOOK (35 186 communes au Sprint 24 → plancher 30 000 dans
  `fetch_bassins.py` ; 101 départements → plancher 90 dans `fetch_refdata.py` ; 100 % de lignes
  écartées dans `build_restrictions.py`). ⚠️ **Conséquence à connaître : au prochain changement de
  schéma amont, la CI de rafraîchissement rougira au lieu de committer un jeu vide.** C'est le
  comportement voulu, mais un plancher mal placé casserait le rafraîchissement des données. **À
  éprouver volontairement une fois** (cf. étape 3 du compte rendu daté).
- ⚠️ **`/api/hydro` a répondu « Service Hub'Eau injoignable pour le moment » sur le déploiement de prod** le 2026-08-05 (diag `prod`, run 35), **alors que `/api/onde` (98 stations, 40 assecs) et `/api/history` répondaient normalement dans le même run**, et que le même code interrogé depuis un runner GitHub trouvait 10 stations autour de Chartres.
  → **Rejoué le 2026-08-06 (run 39) : le symptôme NE se reproduit PAS.** `/api/hydro` répond 200 avec 8 stations réelles autour d'Orléans — la Loire à Orléans - Pont Royal à 0,6 km, dernière mesure du 2026-08-04, référence VCN10 calculée sur 19 ans. Panne transitoire, donc — **mais la route a mis 16,0 s**. À surveiller désormais comme un problème de **latence**, plus comme une panne.
- ⚠️ **La prod est nettement plus lente que le runner GitHub vers Hub'Eau** (mesuré run 39) : `/api/hydro` **16,0 s**, `/api/piezo` **11,0 s**, `/api/bdlisa` **10,8 s**, `/api/history` 9,6 s — quand les routes servies par des données embarquées répondent en 0,15-0,5 s. Le réseau sortant de la fonction serverless, pas le code, est le facteur commun. **Conséquence directe pour le Sprint 32** : le budget de 6 s de `ETAT_REFERENCE_BUDGET_MS` a été corroboré **sur un runner** (3,4 s au pire), or c'est en prod qu'il s'appliquera. ⚠️ **Ce n'est PAS une mesure de `/api/carte/etat` en prod** — `/api/hydro` fait bien davantage (référentiel de bbox + jusqu'à 8 candidates + module + surface de bassin), donc le coût de la seule référence ne s'en déduit pas. C'était un **signal d'alerte à instruire**, pas un verdict — **et le run 40 l'a levé, voir l'entrée de vérification ci-dessus** : si la référence dépasse réellement 6 s en prod, la popup dira « référence non calculée » la plupart du temps, c'est-à-dire qu'elle perdra ce qui faisait tout l'intérêt du sprint. **Premier diag après la mise en prod : chronométrer `/api/carte/etat` depuis Vercel**, et arbitrer le budget sur cette mesure-là.
- **Sprints 12-19 déployés en prod** (`main`, mergés au fil des sessions) — **flux VigiEau/Hub'Eau confirmés en conditions réelles le 2026-07-30** via l'escape hatch diag (mode `anticipation`, voir ci-dessous) : zone VigiEau réelle (Têt aval, 66, niveau vigilance, arrêté du 16/07 au 31/08/2026), station hydro à 0,6 km (donnée du 29/07), piézomètre à 4,2 km (donnée du 19/07). Restent **non encore confirmés en conditions réelles** : **statut ZRE** (`/api/transition`, dépend de `data/refdata/zre-communes.json`), **choroplèthe** (`/api/departements`, dépend de `data/refdata/departements.geojson`), PWA/offline, rendu visuel des rapports ESG et de l'export PDF sur le vrai déploiement Vercel (validés en local/Actions uniquement). Le SW ne s'active qu'en build de prod (pas en dev) → tester installabilité PWA + offline du dashboard sur le preview. Vérifier que les fichiers `data/refdata/*` et `data/projections/*` sont bien embarqués via `outputFileTracingIncludes` (`next.config.ts`).
- **Indice d'anticipation (Sprint 20) validé sur données réelles le 2026-07-30** (`scripts/diag/prod-diag.sh` mode `anticipation` + `scripts/diag/replay-anticipation.ts`) : première exécution non dégradée de `computeAnticipation` — jusque-là seulement testé sur fixtures synthétiques ou en état dégradé côté bac à sable (egress bloqué). Résultat sur Perpignan (Têt aval, 66) début juillet 2026 : niveau **Probable** (3/4), confiance **haute**, moteurs cohérents (base saisonnière 100/100 en pic estival, nappe 44/100 en légère baisse, débit 0/100 au-dessus de l'étiage, statut réglementaire 25/100 en vigilance, trajectoire en retard sur la normale). Le mode `anticipation` est **réutilisable** (même schéma que `app`/`hubeau`) pour re-valider après un changement dans `lib/anticipation.ts` ou `lib/hubeau.ts` — pousser `data/diag-request.json` avec `{"mode":"anticipation"}`, attendre le commit runner, `npx tsx scripts/diag/replay-anticipation.ts`, puis **purger `data/diag/*`** (convention « diag à purger après analyse »).
- Rapport ESG (site, Sprint 17) : la section projection/benchmark n'apparaît que si `/api/projection` répond (egress requis). En bac à sable le rapport se génère sans cette section — comportement attendu, à confirmer complet sur Vercel.
- Répartition géographique (Sprint 16) : le rattachement département dépend du `citycode` du site. Un site enregistré sans citycode (géocodage sans code commune) tombe dans « Département inconnu ». À confirmer sur des sites réels que la BAN renvoie bien le citycode dans tous les cas.
- Le calendrier saisonnier et l'évolution du risque ne s'affichent que si ≥2 années complètes de données sont disponibles — vérifié en logique, mais à confirmer sur des sites réels.
- L'indicateur de confiance ne tient pas encore compte de l'âge des données Onde (saisonnier) ni de la couverture des projections 2050.
- Le secteur n'est pas rétroactivement ajouté aux sites déjà enregistrés — l'utilisateur devra supprimer/recréer ou un futur sprint ajoutera l'édition de sites existants.
- Le manifest PWA ne fournit qu'une icône SVG (`/icon.svg`). Certains anciens Android préfèrent des PNG 192/512 maskables — à ajouter si l'installabilité pose problème sur ces appareils.

## 6. Vérification avant chaque push

```bash
npm run build && npm run lint          # ce que Vercel exécute
# Les 22 suites d'un coup — les énumérer à la main dérive (11 y figuraient sur 17 avant le glob).
for t in scripts/test/*.test.ts; do
  printf "%-28s %s\n" "$(basename "$t")" "$(npx tsx "$t" 2>&1 | tail -1)"
done                                   # npm i --no-save tsx si absent

# Les plus structurantes, si l'on ne doit en lancer que quelques-unes :
npx tsx scripts/test/history-parser.test.ts            # parseur historique, périodes RLE, premiereAnnee
npx tsx scripts/test/portefeuille.test.ts              # simultanéité, HHI, grappes, m³/€, autonomie
npx tsx scripts/test/ressource.test.ts                 # module, débit spécifique, taux WRI, refus motivés
npx tsx scripts/test/executive.test.ts                 # synthèse portefeuille (faits présents/absents)
npx tsx scripts/test/report.test.ts                    # builder du rapport ESG (ESRS E3)
npx tsx scripts/test/carte.test.ts                     # couches de la carte (coordonnées, rayon, positions approchées)
npx tsx scripts/test/hubeau-degrade.test.ts            # panne partielle Hub'Eau ≠ station muette (serveur bouchon)
npx tsx scripts/test/score-indisponible.test.ts        # une source injoignable est nommée, jamais comptée 0

npx next start -p 3300                 # puis (⚠️ fuser -k 3300/tcp pour l'arrêter, cf. §3) :
PLAYWRIGHT_BROWSERS_PATH=/opt/pw-browsers \
  BASE=http://localhost:3300 node scripts/test/e2e.mjs # 62 PASS attendus
```
⚠️ `playwright` n'est pas dans `package.json` : `npm i --no-save playwright` si l'e2e échoue en `ERR_MODULE_NOT_FOUND`. Le navigateur est préinstallé dans `/opt/pw-browsers` — ne pas lancer `playwright install`.
Les APIs françaises échouent en local (egress) : les messages « indisponible » en français dans l'UI sont l'état **attendu** en bac à sable, pas un bug. La validation finale des flux de données se fait sur le preview Vercel.
